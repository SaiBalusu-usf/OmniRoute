# Decision Ledger - OmniRoute-reasoning-fix

## 2026-09-18 - OpenCode Zen Free-Tier Contract: PR #14013 & #14011 + 6-Tool Placeholder Injection

- **Decision:** Apply upstream PR #14013 (request contract for OpenCode Zen free tier) and PR #14011 (refusal classification), and configure `OPENCODE_FREE_TIER_PLACEHOLDER_TOOLS=glob,grep,read,edit,write,bash` in `~/.omniroute/.env`.
- **Reasoning:** OpenCode upstream Console (`/zen/v1`) enforces four strict conditions for free-tier models: `stream: true`, non-empty `tools` array declaring at least 6 recognized official OpenCode client tool names, canonical session IDs (`ses_...`), and versioned User-Agent (`opencode/<semver>`). Falling back to `_noop` failed with 403 because upstream validates tool names against client capabilities. Configuring the 6 proven official client tools allows bare requests to succeed with 200 OK while preserving tool observation learning.
- **Affected Paths:** `open-sse/executors/opencodeFreeTierContract.ts`, `open-sse/executors/opencodeToolObservation.ts`, `open-sse/utils/opencodeHeaders.ts`, `open-sse/executors/opencode.ts`, `open-sse/executors/opencodeAccountHealth.ts`, `open-sse/executors/opencodeGeoBlock.ts`, `open-sse/services/errorClassifier.ts`, `.env.example`, `~/.omniroute/.env`.

## 2026-09-19 - Upstream Merge v3.8.51 (155 Commits) + Astra Custom Invariants Preservation + Release 7cba94f5f

- **Decision:** Merged `upstream/release/v3.8.51` (commit `4d1282be3`) into `release/v3.8.51-astra`, preserving custom zero-headroom memory protections, worker thread `pathToFileURL` loader, `MAX_QUEUE_DEPTH = 32`, explicit field dereferencing on finish/abort, and the 6 placeholder tools for OpenCode free-tier requests. Aligned compression worker unit test to upstream PR #13145 rejection semantics, refactored `chatBodyAdmission.ts` to stay within the frozen file size baseline, and deployed standalone release `7cba94f5f-upstream-astra-20260919T052826Z`.
- **Reasoning:** Integrates 155 upstream commits (including official OpenCode Zen free-tier fixes #14011 and #14013, 16 MiB byte budget & structuredClone usage tracking, stream lifecycle reservations, etc.) without regressing critical host fixes (Next.js 16 Turbopack worker loader path compatibility, memory leak protections, and file-size ratchet limits).
- **Affected Paths:** `open-sse/services/compression/compressionWorkerPool.ts`, `tests/unit/compression/compression-worker.test.ts`, `src/shared/middleware/chatBodyAdmission.ts`, `config/quality/file-size-baseline.json`, `src/lib/usage/completedRequestDetails.ts`, `.env.example`, `decisions.md`.
