# Decision Ledger - OmniRoute-reasoning-fix

## 2026-09-18 - OpenCode Zen Free-Tier Contract: PR #14013 & #14011 + 6-Tool Placeholder Injection

- **Decision:** Apply upstream PR #14013 (request contract for OpenCode Zen free tier) and PR #14011 (refusal classification), and configure `OPENCODE_FREE_TIER_PLACEHOLDER_TOOLS=glob,grep,read,edit,write,bash` in `~/.omniroute/.env`.
- **Reasoning:** OpenCode upstream Console (`/zen/v1`) enforces four strict conditions for free-tier models: `stream: true`, non-empty `tools` array declaring at least 6 recognized official OpenCode client tool names, canonical session IDs (`ses_...`), and versioned User-Agent (`opencode/<semver>`). Falling back to `_noop` failed with 403 because upstream validates tool names against client capabilities. Configuring the 6 proven official client tools allows bare requests to succeed with 200 OK while preserving tool observation learning.
- **Affected Paths:** `open-sse/executors/opencodeFreeTierContract.ts`, `open-sse/executors/opencodeToolObservation.ts`, `open-sse/utils/opencodeHeaders.ts`, `open-sse/executors/opencode.ts`, `open-sse/executors/opencodeAccountHealth.ts`, `open-sse/executors/opencodeGeoBlock.ts`, `open-sse/services/errorClassifier.ts`, `.env.example`, `~/.omniroute/.env`.

## 2026-09-19 - Upstream Merge v3.8.51 (155 Commits) + Astra Custom Invariants Preservation + Release 7cba94f5f

- **Decision:** Merged `upstream/release/v3.8.51` (commit `4d1282be3`) into `release/v3.8.51-astra`, preserving custom zero-headroom memory protections, worker thread `pathToFileURL` loader, `MAX_QUEUE_DEPTH = 32`, explicit field dereferencing on finish/abort, and the 6 placeholder tools for OpenCode free-tier requests. Aligned compression worker unit test to upstream PR #13145 rejection semantics, refactored `chatBodyAdmission.ts` to stay within the frozen file size baseline, and deployed standalone release `7cba94f5f-upstream-astra-20260919T052826Z`.
- **Reasoning:** Integrates 155 upstream commits (including official OpenCode Zen free-tier fixes #14011 and #14013, 16 MiB byte budget & structuredClone usage tracking, stream lifecycle reservations, etc.) without regressing critical host fixes (Next.js 16 Turbopack worker loader path compatibility, memory leak protections, and file-size ratchet limits).
- **Affected Paths:** `open-sse/services/compression/compressionWorkerPool.ts`, `tests/unit/compression/compression-worker.test.ts`, `src/shared/middleware/chatBodyAdmission.ts`, `config/quality/file-size-baseline.json`, `src/lib/usage/completedRequestDetails.ts`, `.env.example`, `decisions.md`.

## 2026-09-19 - OpenCode Zen Free-Tier Client Tool Contract & Tool Observation Precedence Fix

- **Decision:** Updated `open-sse/executors/opencodeFreeTierContract.ts` to append all configured placeholder tools (`glob, grep, read, edit, write, bash`) alongside client-supplied tools (e.g. `run_code` from DeepSeek Harness), and updated `open-sse/executors/opencodeToolObservation.ts` so `configured` placeholder tools take precedence over un-scoped generic observed tools.
- **Reasoning:** Upstream OpenCode Zen free tier (`/zen/v1`) enforces presence of the 6 official client tools even when foreign client tools are present. When a client like DeepSeek Harness sends `run_code`, the contract previously appended only `_noop`, causing upstream to return 403 FreeTierError. Furthermore, an un-scoped observed tool (`run_code`) previously poisoned the placeholder resolver and overwrote configured official tools for subsequent requests. Appending configured placeholders to client tools satisfies upstream without altering client tools, and giving configured tools precedence over un-scoped generic observations prevents tool poisoning.
- **Affected Paths:** `open-sse/executors/opencodeFreeTierContract.ts`, `open-sse/executors/opencodeToolObservation.ts`, `tests/unit/opencode-free-tier-request-contract.test.ts`, `decisions.md`.

## 2026-09-19 - WorkBuddy AI (International / CodeBuddy AI) Integration (Alias `cbai`)

- **Decision:** Added international WorkBuddy AI provider (`codebuddy`, alias `cbai`) alongside `codebuddy-cn` (`cbcn`), providing access to Tencent's global international gateway (`codebuddy.ai` / `workbuddy.ai`) with 21 models (GPT-6-Astra, GPT-5.6 Sol/Terra/Luna, Gemini 3.5 Flash, GLM 5.3, Kimi K3, DeepSeek V4.1 Flash, Hy4/Hy3), global device-code OAuth flow (Google SSO / email, no +86 phone required), 1-click local desktop credential auto-import (`/api/oauth/codebuddy/auto-import`), token refresh, and package usage quota parsing.
- **Reasoning:** CodeBuddy CN relies on `copilot.tencent.com` requiring mainland Chinese phone numbers and WeChat verification. The international version uses the exact same underlying SSE chat completion and token exchange wire format, but operates globally under `workbuddy-ai` platform headers. Local desktop tokens stored at `~/.config/CodeBuddyExtension/Data/Public/auth/workbuddy-desktop-ai.info` can be auto-imported or renewed directly.
- **Affected Paths:** `open-sse/config/providers/registry/codebuddy/index.ts`, `open-sse/config/providers/index.ts`, `open-sse/config/providerHeaderProfiles.ts`, `open-sse/executors/codebuddy.ts`, `open-sse/executors/codebuddy-cn.ts`, `open-sse/executors/index.ts`, `src/lib/oauth/constants/oauth.ts`, `src/lib/oauth/providers/codebuddy.ts`, `src/lib/oauth/providers/index.ts`, `open-sse/services/tokenRefresh/providers/codebuddy.ts`, `open-sse/services/tokenRefresh.ts`, `open-sse/services/usage/codebuddy.ts`, `open-sse/services/usage/supportedProviders.ts`, `open-sse/services/usage/fetcherProviders.ts`, `open-sse/services/usage.ts`, `src/lib/codebuddy/tokenExtractor.ts`, `src/app/api/oauth/codebuddy/auto-import/route.ts`, `src/app/api/oauth/[provider]/[action]/route.ts`, `src/app/api/providers/[id]/test/oauthTestConfig.ts`, `src/shared/constants/publicApiRoutes.ts`, `src/server/authz/routeGuard.ts`, `src/shared/constants/providers.ts`, `src/shared/constants/providers/oauth.ts`, `src/shared/components/OAuthModal.tsx`, `src/shared/components/lobeProviderIcons.ts`, `tests/unit/codebuddy-provider.test.ts`, `decisions.md`.
