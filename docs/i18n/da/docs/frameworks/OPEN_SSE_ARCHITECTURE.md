# open-sse Architecture (Dansk)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Kort fortalt**: `open-sse/` er den centrale streamingmotor, som driver alle LLM-anmodninger i OmniRoute. Den indeholder ~1.400 filer, der implementerer anmodningspipelinen, eksekveringskomponenter, tjenester, MCP-serveren og oversættelseslaget. Denne vejledning forklarer, hvordan delene hænger sammen.

**Kilde:** `open-sse/` (workspace-pakke, ~1.440 `.ts`-filer, som ikke er testfiler)

---

## Hvorfor en separat workspace-pakke?

`open-sse/` er et **selvstændigt workspace** i OmniRoute-monorepoet af flere årsager:

1. **Genbrugelighed** — `open-sse` udgives som `@omniroute/open-sse` på npm, så andre projekter kan bruge den uafhængigt
2. **Klare grænser** — streamingmotoren er afkoblet fra OmniRoute-specifikke UI-/DB-lag
3. **Ydeevne** — motoren har ingen Next.js-afhængigheder, hvilket muliggør hurtigere koldstarter i CLI-/serverless-sammenhænge
4. **Versionsstyring** — `open-sse` kan udgives efter sin egen tidsplan

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Struktur på øverste niveau

```
open-sse/
├── index.ts              # Offentligt indgangspunkt
├── types.d.ts            # Offentlige typeeksporter
├── package.json          # @omniroute/open-sse
├── config/               # Udbyderkonfigurationer, konstanter, registre
├── executors/            # HTTP-eksekveringskomponenter pr. udbyder (67 + base.ts/index.ts)
├── handlers/             # Anmodningshåndteringer (chatCore, responses osv.)
├── lib/                  # Interne hjælpefunktioner
├── mcp-server/           # Model Context Protocol-server
├── services/             # ~298 tjenestemoduler
├── transformer/          # Formattransformering til Responses API
├── translator/           # Formatoversættelse (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Delte hjælpefunktioner (logning, fejl, stream osv.)
```

### Antal moduler

| Mappe | Filer | Formål |
| `executors/` | 167 | HTTP-eksekveringskomponenter pr. udbyder (samlet via DefaultExecutor-factory) |
| `handlers/` | 157 | Indgangspunkter for anmodninger (chatCore, responses, embeddings) |
| `services/` | ~536 | Routing, caching, hastighedsbegrænsning, opdatering osv. |
| `translator/` | 56 | Formatkonvertering (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP-værktøjer og -transportmekanismer |
| `utils/` | ~108 | Tværgående hjælpefunktioner (logning, fejl, stream) |
| `config/` | ~339 | Udbyderkonfigurationer, konstanter, registre |

---

## Anmodningspipelinen

Alle LLM-anmodninger gennemløber en **pipeline med 5 faser**:

```
                              ┌──────────────┐
   HTTP-anmodning              │  1. ROUTE    │   combo-opløsning, modelvalg
   (Next.js-rute)              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. OVERSÆT  │   formatkonvertering (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. EKSEKVER │   udbydereksekvering, HTTP, genforsøg, afbryder
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. STREAM   │   SSE-transformering, modtryk
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. REGISTRER│   brugsregistrering, kaldslog, fejlklassificering
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP-svar (SSE eller JSON)
```

### Fase 1: Routing (services/combo.ts)

**Indgangspunkt**: `handleComboChat()` i `services/combo.ts`

Opløser anmodningen til en konkret tuple af `(provider, model, account, credentials)`:

- Slå comboen op efter ID (eller opbyg en virtuel combo for `auto/*`-modeller)
- Anvend routingstrategien (prioritet, vægtet, round-robin osv.)
- Frasortér udbydere med driftsproblemer (circuit breaker)
- Vælg det næste brugbare mål

For `auto/*`-modeller gør denne fase også følgende:

- Kører algoritmen til **scoring ud fra 16 faktorer** (`services/autoCombo/`)
- Vælger et `provider+model`-par baseret på driftstilstand, omkostninger, latenstid osv.

### Fase 2: Oversættelse (translator/)

Hvis kildeformatet (f.eks. OpenAI) adskiller sig fra målformatet (f.eks. Claude), bliver anmodningen **oversat**:

- Systemprompt → systemmeddelelse
- Værktøjsdefinitioner → udbyderspecifikt værktøjsformat
- Parametre for ræsonnering/tænkning → udbyderspecifikke ækvivalenter
- Normalisering af meddelelsesroller (`developer` → `system` for andre end OpenAI)

`translator/index.ts` eksponerer:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Fase 3: Eksekvering (executors/)

**Indgangspunkt**: `getExecutor(providerId).execute(request, options)`

Alle udbydere bruger `DefaultExecutor` (`executors/default.ts`) via fallback-mekanismen i `getExecutor()`-factoryen. Eksekveringskomponenten:

- Opbygger upstream-URL'en (`buildUrl()`)
- Tilføjer udbyderspecifikke headere (`buildHeaders()`)
- Transformerer anmodningens body (`transformRequest()`)
- Sender HTTP-anmodningen med genforsøg + eksponentiel backoff
- Håndterer opdatering af godkendelse efter behov (OAuth-udbydere)

Alle eksekveringskomponenter udvider `BaseExecutor` (`executors/base.ts`, 1170 kodelinjer), som leverer:

- Fælles logik til genforsøg
- Proxyintegration
- Integration med circuit breaker
- Hooks til registrering af forbrug

### Fase 4: Streaming (utils/stream.ts)

Ved streamede svar returnerer eksekveringskomponenten en **ReadableStream**. Håndteringen:

- Sender data gennem en SSE-transformering (`createSSETransformStreamWithLogger`)
- Anvender heartbeat-pings til at registrere døde forbindelser
- Håndterer afbrydelse fra klienten på en kontrolleret måde (`pipeWithDisconnect`)
- Transformerer SSE → JSON for klienter uden streaming

Ved svar uden streaming returnerer eksekveringskomponenten et parset JSON-objekt, som videresendes uændret.

### Fase 5: Registrering (services/usage.ts)

Efter svaret (uanset om det lykkes eller mislykkes) registreres forbruget:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` fra svaret
- `cost_usd` beregnet ud fra prisdata
- `latency_ms`, `status`, `error_class`, hvis det mislykkes
- Gemmes i tabellen `usage_history`

Artefakter fra kaldeloggen (hvis aktiveret) skrives til `${DATA_DIR}/call_logs/`.

---

## Grundig gennemgang af nøglefiler

### chatCore.ts (5977 linjer)

Den **primære request-handler**. Trods dens størrelse har den en klar struktur:

```ts
// Pseudostruktur for chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Godkendelse + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Validering af body
  const body = await parseRequestBody(request);

  // 3. Formatregistrering + oversættelse
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Combo-routing
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Fortsæt til næste target
    }
  }

  // 5. Nød-fallback
  return await emergencyFallback(body);
}
```

Selvom det er én enorm funktion, er den organiseret i **kommenterede sektioner**, der svarer til pipelinen med 5 trin.

### combo.ts (4456 kodelinjer)

**Routingmotoren**, der omsætter en combo til en ordnet liste af targets.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Understøtter **19 routingstrategier** (se `src/shared/constants/routingStrategies.ts`):

| Strategi            | Adfærd                                                                          |
| ------------------- | ------------------------------------------------------------------------------- |
| `priority`          | Ordnet liste med første target først                                            |
| `weighted`          | Sandsynlighedsbaseret efter vægten for hvert target                             |
| `round-robin`       | Gennemløb targets i rækkefølge                                                  |
| `context-relay`     | Overfør kontekst mellem targets                                                 |
| `fill-first`        | Udfyld kvoten, før der fortsættes til det næste                                 |
| `p2c`               | Bedste af to valg                                                               |
| `random`            | Ensartet tilfældigt valg                                                        |
| `least-used`        | Vælg det target, der har færrest nylige anvendelser                             |
| `cost-optimized`    | Billigste funktionsdygtige target først                                         |
| `reset-aware`       | Tager højde for providernes nulstillingsvinduer                                 |
| `reset-window`      | Routing baseret på nulstillingsvinduer                                          |
| `headroom`          | Største resterende kvotemargen først                                            |
| `strict-random`     | Reelt ensartet tilfældigt valg (ingen kvalitetsvægtning)                        |
| `auto`              | Brug scoring med 16 faktorer (`autoCombo/`)                                     |
| `lkgp`              | Senest kendte funktionsdygtige provider først                                   |
| `context-optimized` | Bedst til requests med lang kontekst                                            |
| `fusion`            | Send parallelt til et panel, og syntetisér derefter via en dommer (`fusion.ts`) |

### base.ts (1170 kodelinjer)

Den **abstrakte executor**, som alle 101 executors udvider. Den indeholder:

- `buildUrl()` — standardkonstruktion af URL (underklasser overskriver ved specialtilpasninger)
- `buildHeaders()` — standardheaders (godkendelse, indholdstype)
- `transformRequest()` — videresendelse uden ændringer som standard
- `execute()` — det primære HTTP-loop med genforsøg/backoff/breaker

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Håndterer alle OpenAI/Anthropic-kompatible providers
  // Providers registrerer konfigurationer (URL, godkendelse, headers), men deler executor-logik
}
```

Providerspecifik adfærd (godkendelsesheaders, basis-URL, versionsheaders) konfigureres via providerregistret og ikke gennem separate executor-klasser.

````

---

## Tjenester (117 moduler)

Tjenester er **fokuserede moduler med ét enkelt formål**, som handlers sammensætter. De overordnede kategorier:

### Routing og kombination

- `combo.ts` — indgangspunkt for combo-routede anmodninger
- `services/autoCombo/` — scoring med 16 faktorer, 8 automatiske routingstrategier
- `wildcardRouter.ts` — matcher wildcard-ruter (`gpt-*`)
- `modelFamilyFallback.ts` — T5-fallback inden for samme familie

### Hastighedsbegrænsning og kvote

- `rateLimitManager.ts` — token bucket pr. nøgle+udbyder
- `usage.ts` — registrering af forbrug
- `quotaCache.ts` — kvoteøjebliksbilleder i hukommelsen

### Konto og token

- `tokenRefresh.ts` — OAuth-fornyelse ved 401
- `accountFallback.ts` — skift til en alternativ konto
- `sessionManager.ts` — sessionstilstand over flere interaktioner

### Intelligens

- `intentClassifier.ts` — klassificerer anmodningens hensigt
- `taskAwareRouter.ts` — router efter opgavetype
- `thinkingBudget.ts` — tildeler tokens til ræsonnering
- `contextManager.ts` — injicerer routingkontekst

### Robusthed

- `resilience.ts` — orkestrering af genforsøg, backoff og breaker
- `emergencyFallback.ts` — fallback som sidste udvej
- `modelDeprecation.ts` — automatisk routing til efterfølgermodeller

### Tilstand

- `signatureCache.ts` — deduplikering efter anmodningssignatur
- `volumeDetector.ts` — belastningsreduktion
- `contextHandoff.ts` — serialisering af sessioner

### Komprimering

- `compression/` (undermappe) — komplet komprimeringspipeline
- 39 filer, der dækker motorer, regelsæt og adaptere

### Færdigheder

- (beskrevet i [SKILLS.md](./SKILLS.md))

### Hukommelse

- (beskrevet i [MEMORY.md](./MEMORY.md))

---

## Eksekveringsmoduler (75+ filer)

Én fil pr. udbyder. De udvider alle `BaseExecutor` og tilsidesætter det, der afviger.

### Fælles mønstre

Udbydere findes via `getExecutor(providerId)`, som returnerer det konfigurerede eksekveringsmodul. OpenAI/Anthropic-kompatible udbydere bruger `DefaultExecutor` (`executors/default.ts`). Udbyderspecifik adfærd (basis-URL, godkendelsesheadere, API-version) konfigureres i `open-sse/config/providers/`, mens transformationer af anmodningens body håndteres i `open-sse/translator/`.

**Brugerdefineret URL** angives via udbyderkonfigurationen:

```ts
// Udbyderkonfiguration i open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Brugerdefineret godkendelse** håndteres via udbyderregistrets godkendelseskonfiguration (API-nøgle, OAuth, headerprofiler).

**Brugerdefinerede transformationer af anmodningens body** (f.eks. at Anthropic adskiller `system` fra `messages`) registreres pr. udbyder i `open-sse/translator/`.

````

### Eksekveringsfabrikken

`executors/index.ts` eksporterer `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Bestemmelsen sker gennem `ExecutorRegistry` (`executors/registry.ts`): Alle specialiserede eksekveringsmoduler deklareres i den indbyggede tabel i `executors/index.ts` og registreres via `registerExecutor(alias, instance)`, når modulet indlæses; `getExecutor()` slår op i registret og falder tilbage til et memoiseret `DefaultExecutor` for enhver udbyder uden en specialiseret post. Den fulde mapping fra alias → eksekveringsmodul er beskrevet af golden-testen `tests/unit/executor-map-golden.test.ts`.

---

## Oversættere

Oversæt mellem **3 formater**: OpenAI, Anthropic, Gemini samt det nye Responses API.

### Hvornår oversættelse finder sted

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Almindelige oversættelser:

- `OpenAI → Anthropic`: separat `system`-felt, `x-api-key`-header
- `OpenAI → Gemini`: `contents` i stedet for `messages`, `systemInstruction`
- `OpenAI → Responses API`: `input`-array, `previous_response_id`-tilstand

### Håndterede særtilfælde

- Rollen `developer` → `system` for andre end OpenAI
- Rollen `system` → flettes ind i den første brugermeddelelse for GLM/ERNIE
- `json_schema` → Geminis `responseMimeType` + `responseSchema`
- `tools` → udbyderspecifikt værktøjsformat
- Thinking-parametre (o1, Claude) → udbyderspecifikke ækvivalenter

---

## MCP-server

`open-sse/mcp-server/` implementerer **Model Context Protocol**-serveren:

- **110 værktøjer** (udbyderadministration, kombinationer, hukommelse, cache, komprimering, proxy, færdigheder, gamification, plugins, Notion, Obsidian, lokalt korpus)
- **3 transportmekanismer**: stdio, SSE, Streamable HTTP
- **33 scopes** til detaljeret godkendelse

### Registrering af værktøjer

Værktøjer registreres som separate filer i `open-sse/mcp-server/tools/`, hvor hver fil eksporterer et navn, et skema, en handler og et scope:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Transportmekanismer

```ts
// stdio (CLI-brug)
startMcpStdio(server);

// SSE (HTTP-baseret streaming)
startMcpSse(server, port);

// Streamable HTTP (moderne MCP)
startMcpStreamable(server, port);
```

### Godkendelse

Hvert værktøjskald gennemgår scope-kontroller (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Transformere

`open-sse/transformer/` konverterer mellem formaterne **Chat Completions** og **Responses API**.

### Hvorfor en separat transformer?

Responses API er OpenAI's nye format med **tilstandsbevarende samtaler** (`previous_response_id`). Når en klient sender en Responses-anmodning, gør OmniRoute følgende:

1. Konverterer internt Responses → Chat Completions
2. Sender til udbyderen (enhver udbyder, der understøtter Chat Completions)
3. Konverterer svaret tilbage til Responses-formatet
4. Streamer det konverterede svar til klienten

Transformeren (`transformer/responsesTransformer.ts`) leverer:

```ts
createResponsesApiTransformStream(): TransformStream
```

Dette håndterer:

- `response.output_item.added`-hændelser
- `response.output_text.delta`-hændelser
- `response.completed`-hændelsen
- Mapping af værktøjskald (`function_call` ↔ `tool_calls`)

---

## Konfiguration

`open-sse/config/` indeholder konfigurationslaget:

| Fil                           | Formål                                            |
| ----------------------------- | ------------------------------------------------- |
| `providerRegistry.ts`         | Chatmodelregister over kataloget med 352 udbydere |
| `providerModels.ts`           | Modelaliasser, formatmapping                      |
| `constants.ts`                | Timeouts, grænser, statuskoder                    |
| `defaultThinkingSignature.ts` | Claudes standard-thinking-signatur                |
| `modelStrip.ts` (i services)  | Udbyderspecifik fjernelse af felter               |

### Skema for udbyderregister

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Zod-validering ved indlæsning af modulet sikrer, at alle udbyderkonfigurationer er gyldige.

---

## Ydeevnekrav

Routingmotoren har strenge ydeevnebudgetter:

| Handling                                      | Mål   | Måling                      |
| --------------------------------------------- | ----- | --------------------------- |
| Kombinationsberegning                         | <10ms | For 50 mål                  |
| Ratebegrænsningskontrol                       | <1ms  | Token bucket i hukommelsen  |
| Fallback for modelfamilie                     | <5ms  | Cachede familiedefinitioner |
| Routing af anmodning                          | <2ms  | Kritisk sti                 |
| **Ingen blokerende I/O i kritisk routingsti** | —     | Alt er asynkront            |

---

## Antimønstre

❌ **Synkrone databasekald i `combo.ts`** — forudberegn og cache
❌ **Logik for gentagne forsøg i handlers** — brug `retry()` fra robusthedstjenesten
❌ **Direkte adgang til providerkonfiguration** — brug getters fra `providerRegistry`
❌ **Hardcodede fallback-kæder** — definer dem i `modelFamilyFallback.ts`
❌ **Tilstandsændringer på tværs af samtidige anmodninger** — brug kun kontekst afgrænset til anmodningen

---

## Tilføjelse af en ny komponent

### Tilføjelse af en ny tjeneste

1. Opret `open-sse/services/[serviceName].ts` med et fokuseret ansvarsområde
2. Eksportér den primære handlerfunktion og eventuelle konstanter
3. Tilføj enhedstests i `tests/unit/services/[serviceName].test.mjs`
4. Integrér den i anmodningspipelinen i `handlers/chatCore.ts` (hvis den er routingrelateret)
5. Opdatér routinglogikken i `combo.ts`, hvis tjenesten påvirker valg af mål
6. Dokumentér den i denne fil

### Tilføjelse af en ny executor

1. Opret `open-sse/executors/[provider].ts`, der udvider `BaseExecutor`
2. Registrér den i `config/providerRegistry.ts`
3. Føj den til factory-funktionen i `executors/index.ts`
4. Tilføj enhedstests for executoren
5. Dokumentér den i `docs/architecture/ARCHITECTURE.md`

### Tilføjelse af et nyt MCP-værktøj

1. Opret eller opdatér `open-sse/mcp-server/tools/[category]Tools.ts`
2. Definér et Zod-skema for input
3. Registrér værktøjet i `mcp-server/index.ts`
4. Føj det til rettighedsmatricen i `mcp-server/auth/`
5. Tilføj enhedstests

---

## Se også

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — overordnet arkitektur
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — udviklerreference
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — mappe for mappe
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — scoring baseret på 16 faktorer
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP-server
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A-server
- Kildekode: `open-sse/` (mere end 400 filer, ca. 143.000 kodelinjer)
