# open-sse Architecture (Suomi)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Lyhyesti**: `open-sse/` on keskeinen suoratoistomoottori, joka käsittelee kaikki OmniRouten LLM-pyynnöt. Se sisältää noin 1 400 tiedostoa, joissa toteutetaan pyyntöputki, suorittimet, palvelut, MCP-palvelin ja käännöskerros. Tässä oppaassa selitetään, miten osat toimivat yhdessä.

**Lähde:** `open-sse/` (työtilapaketti, noin 1 440 muuta kuin testaukseen tarkoitettua `.ts`-tiedostoa)

---

## Miksi erillinen työtilapaketti?

`open-sse/` on **itsenäinen työtila** OmniRoute-monorepositoriossa useista syistä:

1. **Uudelleenkäytettävyys** — `open-sse` julkaistaan npm:ssä nimellä `@omniroute/open-sse`, joten muut projektit voivat käyttää sitä itsenäisesti
2. **Selkeät rajat** — suoratoistomoottori on irrotettu OmniRoute-kohtaisesta käyttöliittymä- ja tietokantakerroksesta
3. **Suorituskyky** — moottorilla ei ole Next.js-riippuvuuksia, mikä mahdollistaa nopeammat kylmäkäynnistykset komentorivi- ja palvelimettomissa ympäristöissä
4. **Versiointi** — `open-sse` voidaan julkaista omassa tahdissaan

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Ylätason rakenne

```
open-sse/
├── index.ts              # Julkinen aloituspiste
├── types.d.ts            # Julkiset tyyppiviennit
├── package.json          # @omniroute/open-sse
├── config/               # Palveluntarjoajien määritykset, vakiot ja rekisterit
├── executors/            # Palveluntarjoajakohtaiset HTTP-suorittimet (67 + base.ts/index.ts)
├── handlers/             # Pyyntökäsittelijät (chatCore, responses jne.)
├── lib/                  # Sisäiset apuohjelmat
├── mcp-server/           # Model Context Protocol -palvelin
├── services/             # Noin 298 palvelumoduulia
├── transformer/          # Responses API -muodon muunnin
├── translator/           # Muotojen välinen käännös (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Jaetut apuohjelmat (lokitus, virheet, suoratoisto jne.)
```

### Moduulien lukumäärät

| Hakemisto | Tiedostot | Tarkoitus |
| `executors/` | 167 | Palveluntarjoajakohtaiset HTTP-suorittimet (yhtenäistetty DefaultExecutor-tehtaan avulla) |
| `handlers/` | 157 | Pyyntöjen aloituspisteet (chatCore, responses, embeddings) |
| `services/` | ~536 | Reititys, välimuisti, nopeusrajoitus, päivitys jne. |
| `translator/` | 56 | Muotojen muuntaminen (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP-työkalut ja siirtomekanismit |
| `utils/` | ~108 | Moduulien yhteiset apuohjelmat (lokitus, virheet, suoratoisto) |
| `config/` | ~339 | Palveluntarjoajien määritykset, vakiot ja rekisterit |

---

## Pyyntöputki

Jokainen LLM-pyyntö kulkee **viisivaiheisen putken** läpi:

```
                              ┌──────────────┐
   HTTP-pyyntö                 │  1. REITITÄ  │   yhdistelmän ratkaisu, mallin valinta
   (Next.js-reitti)            └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. KÄÄNNÄ   │   muodon muuntaminen (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. SUORITA  │   palveluntarjoajan suoritin, HTTP, uudelleenyritys, katkaisija
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. SUORATOISTA│ SSE-muunnos, vastapaine
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. KIRJAA   │   käytön seuranta, kutsuloki, virheiden luokittelu
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP-vastaus (SSE tai JSON)
```

### Vaihe 1: Reititys (services/combo.ts)

**Aloituspiste**: `handleComboChat()` tiedostossa `services/combo.ts`

Ratkaisee pyynnön konkreettiseksi `(provider, model, account, credentials)`-monikoksi:

- Hae yhdistelmä tunnuksella (tai muodosta virtuaalinen yhdistelmä `auto/*`-malleille)
- Käytä reititysstrategiaa (prioriteetti, painotettu, vuorottelu jne.)
- Suodata pois epäkuntoiset palveluntarjoajat (katkaisija)
- Valitse seuraava käyttökelpoinen kohde

`auto/*`-mallien tapauksessa tämä vaihe myös:

- Suorittaa **16 tekijän pisteytysalgoritmin** (`services/autoCombo/`)
- Valitsee `provider+model`-parin toimintakunnon, kustannusten, viiveen ja muiden tekijöiden perusteella

### Vaihe 2: Käännös (translator/)

Jos lähdemuoto (esimerkiksi OpenAI) poikkeaa kohdemuodosta (esimerkiksi Claude), pyyntö **käännetään**:

- Järjestelmäkehote → järjestelmäviesti
- Työkalumääritykset → palveluntarjoajakohtainen työkalumuoto
- Päättely-/ajatteluparametrit → palveluntarjoajakohtaiset vastineet
- Viestiroolien normalisointi (`developer` → `system` muille kuin OpenAI-palveluntarjoajille)

`translator/index.ts` tarjoaa seuraavat:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Vaihe 3: Suoritus (executors/)

**Aloituspiste**: `getExecutor(providerId).execute(request, options)`

Kaikki palveluntarjoajat käyttävät `DefaultExecutor`-suoritinta (`executors/default.ts`) `getExecutor()`-tehtaan vararatkaisun kautta. Suoritin:

- Muodostaa ylävirran URL-osoitteen (`buildUrl()`)
- Lisää palveluntarjoajakohtaiset otsakkeet (`buildHeaders()`)
- Muuntaa pyynnön rungon (`transformRequest()`)
- Lähettää HTTP-pyynnön käyttäen uudelleenyrityksiä ja eksponentiaalista viivettä
- Käsittelee tarvittaessa todennuksen päivittämisen (OAuth-palveluntarjoajat)

Kaikki suorittimet laajentavat `BaseExecutor`-luokkaa (`executors/base.ts`, 1170 koodiriviä), joka tarjoaa:

- Yhteisen uudelleenyrityslogiikan
- Välityspalvelinintegraation
- Katkaisijaintegraation
- Käytön kirjaamiskoukut

### Vaihe 4: Suoratoisto (utils/stream.ts)

Suoratoistettavia vastauksia varten suoritin palauttaa **ReadableStream**-virran. Käsittelijä:

- Ohjaa virran SSE-muunnoksen läpi (`createSSETransformStreamWithLogger`)
- Käyttää sykeviestejä kuolleiden yhteyksien havaitsemiseen
- Käsittelee asiakkaan yhteyden katkeamisen hallitusti (`pipeWithDisconnect`)
- Muuntaa SSE:n JSON-muotoon asiakkaille, jotka eivät käytä suoratoistoa

Muiden kuin suoratoistettavien vastausten tapauksessa suoritin palauttaa jäsennetyn JSON-objektin, joka välitetään muuttamattomana.

### Vaihe 5: Kirjaaminen (services/usage.ts)

Vastauksen jälkeen (onnistui tai epäonnistui) käyttötiedot tallennetaan:

- Vastauksesta saadut `prompt_tokens`-, `completion_tokens`- ja `cached_tokens`-arvot
- Hinnoittelutiedoista laskettu `cost_usd`
- `latency_ms`, `status` ja epäonnistumisen yhteydessä `error_class`
- Tallennetaan `usage_history`-tauluun

Kutsulokien artefaktit (jos käytössä) kirjoitetaan hakemistoon `${DATA_DIR}/call_logs/`.

---

## Keskeisten tiedostojen perusteellinen tarkastelu

### chatCore.ts (5977 riviä)

**Pääasiallinen pyyntöjen käsittelijä**. Koostaan huolimatta sillä on selkeä rakenne:

```ts
// chatCore.ts-tiedoston pseudorakenne
export async function handleChat(request: NextRequest) {
  // 1. Todennus + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Pyynnön rungon validointi
  const body = await parseRequestBody(request);

  // 3. Muodon tunnistus + muunnos
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Yhdistelmän reititys
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Jatka seuraavaan kohteeseen
    }
  }

  // 5. Hätävaravaihtoehto
  return await emergencyFallback(body);
}
```

Vaikka kyseessä on yksi valtava funktio, se on järjestetty **kommentoituihin osioihin**, jotka vastaavat viisivaiheista käsittelyputkea.

### combo.ts (4456 LOC)

**Reititysmoottori**, joka muuntaa yhdistelmän järjestetyksi kohdeluetteloksi.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Tukee **19 reititysstrategiaa** (katso `src/shared/constants/routingStrategies.ts`):

| Strategia           | Toiminta                                                                                            |
| ------------------- | --------------------------------------------------------------------------------------------------- |
| `priority`          | Ensimmäisen kohteen mukaan järjestetty luettelo                                                     |
| `weighted`          | Todennäköisyyspohjainen valinta kohdekohtaisten painoarvojen perusteella                            |
| `round-robin`       | Kohteiden vuorottelu järjestyksessä                                                                 |
| `context-relay`     | Kontekstin siirtäminen kohteiden välillä                                                            |
| `fill-first`        | Kiintiön täyttäminen ennen seuraavaan siirtymistä                                                   |
| `p2c`               | Kahden vaihtoehdon menetelmä                                                                        |
| `random`            | Tasainen satunnaisvalinta                                                                           |
| `least-used`        | Valitaan kohde, jota on käytetty viime aikoina vähiten                                              |
| `cost-optimized`    | Edullisin toimintakuntoinen kohde ensin                                                             |
| `reset-aware`       | Huomioi palveluntarjoajien nollausikkunat                                                           |
| `reset-window`      | Nollausikkunoihin perustuva reititys                                                                |
| `headroom`          | Kohde, jolla on eniten käyttämätöntä kiintiötä, valitaan ensin                                      |
| `strict-random`     | Aidosti tasainen satunnaisvalinta (ei laatupainotusta)                                              |
| `auto`              | Käyttää 16 tekijän pisteytystä (`autoCombo/`)                                                       |
| `lkgp`              | Viimeisin tunnetusti toimiva palveluntarjoaja ensin                                                 |
| `context-optimized` | Soveltuu parhaiten pitkän kontekstin pyyntöihin                                                     |
| `fusion`            | Lähettää pyynnön rinnakkain paneelille ja koostaa sitten vastauksen arviointimallilla (`fusion.ts`) |

### base.ts (1170 LOC)

**Abstrakti suorittaja**, jonka kaikki 101 suorittajaa laajentavat. Se sisältää seuraavat:

- `buildUrl()` — URL-osoitteen oletusmuodostus (aliluokat ohittavat mukautettuja tarpeita varten)
- `buildHeaders()` — oletusotsakkeet (todennus, sisältötyyppi)
- `transformRequest()` — oletusarvoisesti välittää pyynnön muuttamattomana
- `execute()` — HTTP-pääsilmukka, joka sisältää uudelleenyritykset, eksponentiaalisen viiveen ja katkaisijan

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Käsittelee kaikki OpenAI-/Anthropic-yhteensopivat palveluntarjoajat
  // Palveluntarjoajat rekisteröivät määritykset (URL, todennus, otsakkeet), mutta jakavat suorituslogiikan
}
```

Palveluntarjoajakohtainen toiminta (todennusotsakkeet, perus-URL-osoite, versio-otsakkeet) määritetään palveluntarjoajarekisterin kautta erillisten suorittajaluokkien sijaan.

````

---

## Palvelut (117 moduulia)

Palvelut ovat **tarkasti rajattuja, yhden tarkoituksen moduuleja**, joita käsittelijät yhdistelevät. Pääkategoriat:

### Reititys ja yhdistelmät

- `combo.ts` — yhdistelmäreititettyjen pyyntöjen aloituspiste
- `services/autoCombo/` — 16 tekijän pisteytys, 8 automaattista reititysstrategiaa
- `wildcardRouter.ts` — vastaa jokerimerkkireitteihin (`gpt-*`)
- `modelFamilyFallback.ts` — T5:n malliperheen sisäinen varajärjestely

### Nopeusrajoitus ja kiintiöt

- `rateLimitManager.ts` — avain- ja palveluntarjoajakohtainen token bucket
- `usage.ts` — käytön kirjaaminen
- `quotaCache.ts` — muistissa säilytettävät kiintiötilannekuvat

### Tilit ja tunnukset

- `tokenRefresh.ts` — OAuth-tunnuksen uusiminen 401-vastauksen yhteydessä
- `accountFallback.ts` — vaihtaminen vaihtoehtoiseen tiliin
- `sessionManager.ts` — monivuoroisen istunnon tila

### Älykkyys

- `intentClassifier.ts` — pyynnön tarkoituksen luokittelu
- `taskAwareRouter.ts` — reititys tehtävätyypin mukaan
- `thinkingBudget.ts` — päättelytokenien kohdentaminen
- `contextManager.ts` — reitityskontekstin lisääminen

### Vikasietoisuus

- `resilience.ts` — uudelleenyritysten, viiveiden ja katkaisijan orkestrointi
- `emergencyFallback.ts` — viimeisenä keinona käytettävä varajärjestely
- `modelDeprecation.ts` — automaattinen reititys seuraajamalleihin

### Tila

- `signatureCache.ts` — duplikaattien poisto pyynnön allekirjoituksen perusteella
- `volumeDetector.ts` — kuormituksen keventäminen
- `contextHandoff.ts` — istunnon serialisointi

### Pakkaus

- `compression/` (alihakemisto) — täydellinen pakkausputki
- 39 tiedostoa, jotka kattavat moottorit, sääntökokoelmat ja sovittimet

### Taidot

- (käsitellään tiedostossa [SKILLS.md](./SKILLS.md))

### Muisti

- (käsitellään tiedostossa [MEMORY.md](./MEMORY.md))

---

## Suorittajat (yli 75 tiedostoa)

Yksi tiedosto palveluntarjoajaa kohden. Ne kaikki laajentavat `BaseExecutor`-luokkaa ja korvaavat toisistaan poikkeavat osat.

### Yleiset mallit

Palveluntarjoajat ratkaistaan kutsulla `getExecutor(providerId)`, joka palauttaa määritetyn suorittajan. OpenAI-/Anthropic-yhteensopivat palveluntarjoajat käyttävät `DefaultExecutor`-suorittajaa (`executors/default.ts`). Palveluntarjoajakohtainen toiminta (perus-URL-osoite, todennusotsakkeet ja API-versio) määritetään hakemistossa `open-sse/config/providers/`, kun taas pyyntörungon muunnokset käsitellään hakemistossa `open-sse/translator/`.

**Mukautettu URL-osoite** asetetaan palveluntarjoajan määrityksissä:

```ts
// Palveluntarjoajan määritykset hakemistossa open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Mukautettu todennus** käsitellään palveluntarjoajarekisterin todennusmäärityksillä (API-avain, OAuth ja otsakeprofiilit).

**Mukautetut pyyntörungon muunnokset** (esimerkiksi Anthropic, joka erottaa `system`-kentän `messages`-kentästä) rekisteröidään palveluntarjoajakohtaisesti hakemistossa `open-sse/translator/`.

````

### Suorittajatehdas

`executors/index.ts` vie `getExecutor(providerId)`-funktion:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Ratkaisu tapahtuu `ExecutorRegistry`-rekisterin (`executors/registry.ts`) kautta: jokainen erikoistunut suorittaja määritellään tiedoston `executors/index.ts` sisäänrakennetussa taulukossa ja rekisteröidään kutsulla `registerExecutor(alias, instance)` moduulin latauksen yhteydessä; `getExecutor()` tarkistaa rekisterin ja käyttää varavaihtoehtona memoisoitua `DefaultExecutor`-suorittajaa palveluntarjoajille, joilla ei ole erikoistunutta merkintää. Täydellinen alias → suorittaja -vastaavuus määritellään golden-testissä `tests/unit/executor-map-golden.test.ts`.

---

## Kääntäjät

Muuntaa **3 muodon** välillä: OpenAI, Anthropic, Gemini sekä uusi Responses API.

### Milloin muunnos tehdään

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Yleisiä muunnoksia:

- `OpenAI → Anthropic`: erillinen `system`-kenttä, `x-api-key`-otsake
- `OpenAI → Gemini`: `contents` kentän `messages` sijaan, `systemInstruction`
- `OpenAI → Responses API`: `input`-taulukko, `previous_response_id`-tila

### Käsitellyt reunatapaukset

- `developer`-rooli → `system` muille kuin OpenAI:lle
- `system`-rooli → yhdistetään ensimmäiseen käyttäjäviestiin GLM:ää/ERNIEä varten
- `json_schema` → Geminin `responseMimeType` + `responseSchema`
- `tools` → palveluntarjoajakohtainen työkalumuoto
- Päättelyparametrit (o1, Claude) → palveluntarjoajakohtaiset vastineet

---

## MCP-palvelin

`open-sse/mcp-server/` toteuttaa **Model Context Protocol** -palvelimen:

- **110 työkalua** (palveluntarjoajien hallinta, yhdistelmät, muisti, välimuisti, pakkaus, välityspalvelin, taidot, pelillistäminen, liitännäiset, Notion, Obsidian, paikallinen aineisto)
- **3 siirtotapaa**: stdio, SSE, Streamable HTTP
- **33 käyttöoikeusaluetta** tarkkarajaiseen valtuutukseen

### Työkalujen rekisteröinti

Työkalut rekisteröidään erillisinä tiedostoina hakemistossa `open-sse/mcp-server/tools/`. Kukin niistä vie nimen, skeeman, käsittelijän ja käyttöoikeusalueen:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Siirtotavat

```ts
// stdio (CLI-käyttö)
startMcpStdio(server);

// SSE (HTTP-pohjainen suoratoisto)
startMcpSse(server, port);

// Streamable HTTP (nykyaikainen MCP)
startMcpStreamable(server, port);
```

### Valtuutus

Jokainen työkalukutsu käy läpi käyttöoikeusalueiden tarkistukset (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Muuntimet

`open-sse/transformer/` muuntaa **Chat Completions**- ja **Responses API** -muotojen välillä.

### Miksi erillinen muunnin?

Responses API on OpenAI:n uusi muoto, joka tukee **tilallisia keskusteluja** (`previous_response_id`). Kun asiakas lähettää Responses-pyynnön, OmniRoute:

1. Muuntaa Responses-muodon sisäisesti Chat Completions -muotoon
2. Lähettää pyynnön palveluntarjoajalle (mille tahansa Chat Completions -muotoa tukevalle palveluntarjoajalle)
3. Muuntaa vastauksen takaisin Responses-muotoon
4. Suoratoistaa muunnetun vastauksen asiakkaalle

Muunnin (`transformer/responsesTransformer.ts`) tarjoaa seuraavan:

```ts
createResponsesApiTransformStream(): TransformStream
```

Se käsittelee seuraavat:

- `response.output_item.added`-tapahtumat
- `response.output_text.delta`-tapahtumat
- `response.completed`-tapahtuma
- Työkalukutsujen vastaavuus (`function_call` ↔ `tool_calls`)

---

## Määritykset

`open-sse/config/` sisältää määrityskerroksen:

| Tiedosto                      | Tarkoitus                                                       |
| ----------------------------- | --------------------------------------------------------------- |
| `providerRegistry.ts`         | Keskustelumallirekisteri 352 palveluntarjoajan luettelon päällä |
| `providerModels.ts`           | Mallien aliakset ja muotojen vastaavuudet                       |
| `constants.ts`                | Aikakatkaisut, rajat ja tilakoodit                              |
| `defaultThinkingSignature.ts` | Clauden oletusarvoinen päättelyallekirjoitus                    |
| `modelStrip.ts` (palveluissa) | Palveluntarjoajakohtainen kenttien poistaminen                  |

### Palveluntarjoajarekisterin skeema

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Moduulin latauksen yhteydessä suoritettava Zod-validointi varmistaa, että kaikki palveluntarjoajien määritykset ovat kelvollisia.

---

## Suorituskykyrajoitteet

Reititysmoottorilla on tiukat suorituskykyrajat:

| Toiminto                                                       | Tavoite | Mittaus                          |
| -------------------------------------------------------------- | ------- | -------------------------------- |
| Yhdistelmän ratkaisu                                           | <10ms   | 50 kohteelle                     |
| Nopeusrajoituksen tarkistus                                    | <1ms    | Muistissa oleva token bucket     |
| Malliperheen varajärjestely                                    | <5ms    | Välimuistissa olevat määritykset |
| Reitityspyynnön välitys                                        | <2ms    | Kriittinen suorituspolku         |
| **Ei estävää I/O:ta reitityksen kriittisellä suorituspolulla** | —       | Kaikki asynkronista              |

---

## Vältettävät toimintamallit

❌ **Synkroniset tietokantakutsut tiedostossa `combo.ts`** — esilaske ja tallenna välimuistiin
❌ **Uudelleenyrityslogiikka käsittelijöissä** — käytä vikasietoisuuspalvelun `retry()`-toimintoa
❌ **Suora pääsy palveluntarjoajan määrityksiin** — käytä `providerRegistry`-gettereitä
❌ **Kovakoodatut varaketjut** — määritä ne tiedostossa `modelFamilyFallback.ts`
❌ **Tilan muutokset samanaikaisten pyyntöjen välillä** — käytä vain pyyntökohtaista kontekstia

---

## Uuden komponentin lisääminen

### Uuden palvelun lisääminen

1. Luo `open-sse/services/[serviceName].ts`, jolla on tarkasti rajattu vastuu
2. Vie pääkäsittelijäfunktio ja mahdolliset vakiot
3. Lisää yksikkötestit tiedostoon `tests/unit/services/[serviceName].test.mjs`
4. Integroi palvelu pyyntöputkeen tiedostossa `handlers/chatCore.ts` (jos se liittyy reititykseen)
5. Päivitä reitityslogiikka tiedostossa `combo.ts`, jos palvelu vaikuttaa kohteen valintaan
6. Dokumentoi tässä tiedostossa

### Uuden suorittimen lisääminen

1. Luo `open-sse/executors/[provider].ts`, joka laajentaa `BaseExecutor`-luokkaa
2. Rekisteröi tiedostossa `config/providerRegistry.ts`
3. Lisää tiedoston `executors/index.ts` tehtaaseen
4. Lisää suorittimelle yksikkötestit
5. Dokumentoi tiedostossa `docs/architecture/ARCHITECTURE.md`

### Uuden MCP-työkalun lisääminen

1. Luo tai päivitä `open-sse/mcp-server/tools/[category]Tools.ts`
2. Määritä syötteille Zod-skeema
3. Rekisteröi työkalu tiedostossa `mcp-server/index.ts`
4. Lisää käyttöoikeusmatriisiin hakemistossa `mcp-server/auth/`
5. Lisää yksikkötestit

---

## Katso myös

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — yleisarkkitehtuuri
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — tekninen viite
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — hakemistokohtainen kuvaus
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — 16 tekijän pisteytys
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP-palvelin
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A-palvelin
- Lähdekoodi: `open-sse/` (yli 400 tiedostoa, noin 143 000 koodiriviä)
