# open-sse Architecture (ខ្មែរ)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **សង្ខេបខ្លី**៖ `open-sse/` គឺជាម៉ាស៊ីនស្ទ្រីមស្នូលដែលដំណើរការរាល់សំណើ LLM នៅក្នុង OmniRoute។ វាមានឯកសារប្រហែល 1,400 ដែលអនុវត្តខ្សែដំណើរការសំណើ កម្មវិធីប្រតិបត្តិ សេវាកម្ម ម៉ាស៊ីនមេ MCP និងស្រទាប់បកប្រែទម្រង់។ មគ្គុទ្ទេសក៍នេះពន្យល់ពីរបៀបដែលផ្នែកទាំងនេះដំណើរការរួមគ្នា។

**ប្រភព៖** `open-sse/` (កញ្ចប់ workspace ដែលមានឯកសារ `.ts` មិនមែនសម្រាប់តេស្តប្រហែល 1,440)

---

## ហេតុអ្វីត្រូវមានកញ្ចប់ Workspace ដាច់ដោយឡែក?

`open-sse/` គឺជា **workspace ឯករាជ្យ** មួយនៅក្នុង monorepo របស់ OmniRoute ដោយសារហេតុផលជាច្រើន៖

1. **ការប្រើប្រាស់ឡើងវិញ** — `open-sse` ត្រូវបានចេញផ្សាយជា `@omniroute/open-sse` នៅលើ npm ដូច្នេះគម្រោងផ្សេងទៀតអាចប្រើវាដោយឯករាជ្យបាន
2. **ព្រំដែនច្បាស់លាស់** — ម៉ាស៊ីនស្ទ្រីមត្រូវបានផ្តាច់ចេញពីស្រទាប់ UI/DB ដែលជាក់លាក់សម្រាប់ OmniRoute
3. **ប្រសិទ្ធភាព** — ម៉ាស៊ីននេះមិនមាន dependency លើ Next.js ទេ ដែលអនុញ្ញាតឱ្យចាប់ផ្ដើមពីស្ថានភាពត្រជាក់បានលឿនជាងមុនក្នុងបរិបទ CLI/serverless
4. **ការគ្រប់គ្រងកំណែ** — `open-sse` អាចចេញកំណែតាមកាលវិភាគរបស់ខ្លួន

```json
// package.json
"workspaces": ["open-sse"]
```

---

## រចនាសម្ព័ន្ធកម្រិតកំពូល

```
open-sse/
├── index.ts              # ចំណុចចូលប្រើសាធារណៈ
├── types.d.ts            # ការនាំចេញ type សាធារណៈ
├── package.json          # @omniroute/open-sse
├── config/               # ការកំណត់រចនាសម្ព័ន្ធ provider, ថេរ និងបញ្ជីចុះឈ្មោះ
├── executors/            # កម្មវិធីប្រតិបត្តិ HTTP សម្រាប់ provider នីមួយៗ (67 + base.ts/index.ts)
├── handlers/             # កម្មវិធីដោះស្រាយសំណើ (chatCore, responses ជាដើម)
├── lib/                  # ឧបករណ៍ប្រើប្រាស់ផ្ទៃក្នុង
├── mcp-server/           # ម៉ាស៊ីនមេ Model Context Protocol
├── services/             # ម៉ូឌុលសេវាកម្មប្រហែល 298
├── transformer/          # កម្មវិធីបម្លែងទម្រង់ Responses API
├── translator/           # ការបកប្រែទម្រង់ (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # ឧបករណ៍ប្រើប្រាស់រួម (ការកត់ត្រា កំហុស ស្ទ្រីម ជាដើម)
```

### ចំនួនម៉ូឌុល

| ថត | ឯកសារ | គោលបំណង |
| `executors/` | 167 | កម្មវិធីប្រតិបត្តិ HTTP សម្រាប់ provider នីមួយៗ (បង្រួបបង្រួមតាមរយៈ factory របស់ DefaultExecutor) |
| `handlers/` | 157 | ចំណុចចូលសំណើ (chatCore, responses, embeddings) |
| `services/` | ~536 | ការកំណត់ផ្លូវ ឃ្លាំងសម្ងាត់ ការកំណត់អត្រា ការធ្វើឱ្យស្រស់ ជាដើម |
| `translator/` | 56 | ការបម្លែងទម្រង់ (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | ឧបករណ៍ និងមធ្យោបាយបញ្ជូន MCP |
| `utils/` | ~108 | ឧបករណ៍ប្រើប្រាស់ឆ្លងកាត់ផ្នែកផ្សេងៗ (ការកត់ត្រា កំហុស ស្ទ្រីម) |
| `config/` | ~339 | ការកំណត់រចនាសម្ព័ន្ធ provider, ថេរ និងបញ្ជីចុះឈ្មោះ |

---

## ខ្សែដំណើរការសំណើ

រាល់សំណើ LLM ឆ្លងកាត់ **ខ្សែដំណើរការ 5 ដំណាក់កាល**៖

```
                              ┌──────────────┐
   សំណើ HTTP                   │  1. កំណត់ផ្លូវ │   ដោះស្រាយ combo, ជ្រើសរើស model
   (route របស់ Next.js)        └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. បកប្រែ    │   បម្លែងទម្រង់ (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. ប្រតិបត្តិ │   កម្មវិធីប្រតិបត្តិ provider, HTTP, retry, breaker
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. ស្ទ្រីម    │   ការបម្លែង SSE, backpressure
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. កត់ត្រា   │   តាមដានការប្រើប្រាស់ កំណត់ហេតុ call ចាត់ថ្នាក់កំហុស
                              └──────┬───────┘
                                     │
                                     ▼
                              ការឆ្លើយតប HTTP (SSE ឬ JSON)
```

### ដំណាក់កាលទី 1៖ កំណត់ផ្លូវ (services/combo.ts)

**ចំណុចចូល**៖ `handleComboChat()` នៅក្នុង `services/combo.ts`

ដោះស្រាយសំណើទៅជា tuple ជាក់លាក់មួយ `(provider, model, account, credentials)`៖

- ស្វែងរក combo តាម ID (ឬបង្កើត combo និម្មិតសម្រាប់ model `auto/*`)
- អនុវត្តយុទ្ធសាស្ត្រកំណត់ផ្លូវ (អាទិភាព ទម្ងន់ round-robin ជាដើម)
- ច្រោះ provider ដែលមិនមានស្ថានភាពល្អចេញ (circuit breaker)
- ជ្រើសរើសគោលដៅបន្ទាប់ដែលអាចប្រើបាន

សម្រាប់ model `auto/*` ដំណាក់កាលនេះក៏៖

- ដំណើរការអាល់ហ្គរីធម៍ **ដាក់ពិន្ទុដោយផ្អែកលើកត្តា 16** (`services/autoCombo/`)
- ជ្រើសរើសគូ `provider+model` ដោយផ្អែកលើស្ថានភាព ប្រសិទ្ធភាព ថ្លៃចំណាយ latency ជាដើម

### ដំណាក់កាលទី 2៖ បកប្រែ (translator/)

ប្រសិនបើទម្រង់ប្រភព (ឧទាហរណ៍ OpenAI) ខុសពីទម្រង់គោលដៅ (ឧទាហរណ៍ Claude) សំណើនឹងត្រូវបាន **បកប្រែ**៖

- System prompt → system message
- និយមន័យឧបករណ៍ → ទម្រង់ឧបករណ៍ជាក់លាក់របស់ provider
- ប៉ារ៉ាម៉ែត្រ reasoning/thinking → តម្លៃសមមូលជាក់លាក់របស់ provider
- ធ្វើឱ្យ message role មានលក្ខណៈស្តង់ដារ (`developer` → `system` សម្រាប់ប្រព័ន្ធដែលមិនមែនជា OpenAI)

`translator/index.ts` បង្ហាញមុខងារខាងក្រោម៖

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### ដំណាក់កាលទី 3៖ ប្រតិបត្តិ (executors/)

**ចំណុចចូល**៖ `getExecutor(providerId).execute(request, options)`

provider ទាំងអស់ប្រើ `DefaultExecutor` (`executors/default.ts`) តាមរយៈ fallback របស់ factory `getExecutor()`។ កម្មវិធីប្រតិបត្តិនេះ៖

- បង្កើត URL ខ្សែខាងលើ (`buildUrl()`)
- បន្ថែម header ជាក់លាក់របស់ provider (`buildHeaders()`)
- បម្លែង request body (`transformRequest()`)
- ផ្ញើសំណើ HTTP ជាមួយ retry + exponential backoff
- ដោះស្រាយការធ្វើឱ្យ auth ស្រស់ឡើងវិញប្រសិនបើចាំបាច់ (provider ដែលប្រើ OAuth)

កម្មវិធីប្រតិបត្តិទាំងអស់ពង្រីកពី `BaseExecutor` (`executors/base.ts`, 1170 LOC) ដែលផ្ដល់នូវ៖

- តក្កវិជ្ជា retry រួម
- ការរួមបញ្ចូល proxy
- ការរួមបញ្ចូល circuit breaker
- hook សម្រាប់កត់ត្រាការប្រើប្រាស់

### ដំណាក់កាលទី 4៖ ស្ទ្រីម (utils/stream.ts)

សម្រាប់ការឆ្លើយតបជាស្ទ្រីម កម្មវិធីប្រតិបត្តិត្រឡប់ **ReadableStream** មួយ។ កម្មវិធីដោះស្រាយ៖

- បញ្ជូនតាមរយៈការបម្លែង SSE (`createSSETransformStreamWithLogger`)
- អនុវត្ត heartbeat ping ដើម្បីរកឃើញការតភ្ជាប់ដែលលែងដំណើរការ
- ដោះស្រាយការផ្ដាច់ការតភ្ជាប់របស់ client ដោយរលូន (`pipeWithDisconnect`)
- បម្លែង SSE → JSON សម្រាប់ client ដែលមិនប្រើស្ទ្រីម

សម្រាប់ការឆ្លើយតបដែលមិនមែនជាស្ទ្រីម កម្មវិធីប្រតិបត្តិត្រឡប់ object JSON ដែលបាន parse រួច ហើយវាត្រូវបានបញ្ជូនបន្តដោយមិនមានការផ្លាស់ប្ដូរ។

### ដំណាក់កាលទី 5៖ កត់ត្រា (services/usage.ts)

បន្ទាប់ពីការឆ្លើយតប (ជោគជ័យ ឬបរាជ័យ) ការប្រើប្រាស់ត្រូវបានកត់ត្រា៖

- `prompt_tokens`, `completion_tokens`, `cached_tokens` ពីការឆ្លើយតប
- `cost_usd` ត្រូវបានគណនាពីទិន្នន័យតម្លៃ
- `latency_ms`, `status`, `error_class` ប្រសិនបើបរាជ័យ
- រក្សាទុកក្នុងតារាង `usage_history`

វត្ថុបុរាណកំណត់ហេតុនៃការហៅ (ប្រសិនបើបានបើកប្រើ) ត្រូវបានសរសេរទៅ `${DATA_DIR}/call_logs/`។

---

## ការពិនិត្យស៊ីជម្រៅលើឯកសារសំខាន់ៗ

### chatCore.ts (5977 បន្ទាត់)

**កម្មវិធីដោះស្រាយសំណើចម្បង**។ ទោះបីជាវាមានទំហំធំក៏ដោយ វាមានរចនាសម្ព័ន្ធច្បាស់លាស់៖

```ts
// រចនាសម្ព័ន្ធក្លែងក្លាយរបស់ chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. ការផ្ទៀងផ្ទាត់អត្តសញ្ញាណ + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. ការផ្ទៀងផ្ទាត់ body
  const body = await parseRequestBody(request);

  // 3. ការរកឃើញទម្រង់ + ការបកប្រែ
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. ការកំណត់ផ្លូវ combo
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // បន្តទៅកាន់ target បន្ទាប់
    }
  }

  // 5. ជម្រើសបម្រុងក្នុងស្ថានភាពបន្ទាន់
  return await emergencyFallback(body);
}
```

ទោះបីជាវាជា function ដ៏ធំតែមួយក៏ដោយ វាត្រូវបានរៀបចំជា **ផ្នែកដែលមាន comment** ដែលផ្គូផ្គងទៅនឹង pipeline 5 ដំណាក់កាល។

### combo.ts (4456 LOC)

**ម៉ាស៊ីនកំណត់ផ្លូវ** ដែលបម្លែង combo ទៅជា targets ដែលបានតម្រៀបតាមលំដាប់។

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

គាំទ្រ **យុទ្ធសាស្ត្រកំណត់ផ្លូវចំនួន 19** (សូមមើល `src/shared/constants/routingStrategies.ts`)៖

| យុទ្ធសាស្ត្រ        | ឥរិយាបថ                                                                        |
| ------------------- | ------------------------------------------------------------------------------ |
| `priority`          | បញ្ជីដែលបានតម្រៀបដោយផ្តល់អាទិភាពដល់ target ដំបូង                               |
| `weighted`          | ជ្រើសរើសតាមប្រូបាប៊ីលីតេដោយផ្អែកលើទម្ងន់របស់ target នីមួយៗ                     |
| `round-robin`       | ប្ដូរវេនឆ្លងកាត់ targets តាមលំដាប់                                             |
| `context-relay`     | ផ្ទេរ context ឆ្លងកាត់ targets                                                 |
| `fill-first`        | ប្រើ quota ឱ្យពេញ មុនពេលបន្តទៅកាន់ target បន្ទាប់                              |
| `p2c`               | អំណាចនៃជម្រើសពីរ                                                               |
| `random`            | ចៃដន្យស្មើៗគ្នា                                                                |
| `least-used`        | ជ្រើសរើស target ដែលមានការប្រើប្រាស់ថ្មីៗតិចបំផុត                               |
| `cost-optimized`    | ជ្រើសរើស target ដែលដំណើរការល្អ និងថោកបំផុតមុន                                  |
| `reset-aware`       | យល់ដឹងអំពីចន្លោះពេល reset របស់ provider                                        |
| `reset-window`      | ការកំណត់ផ្លូវផ្អែកលើចន្លោះពេល reset                                            |
| `headroom`          | ជ្រើសរើស quota ដែលនៅសល់ច្រើនបំផុតមុន                                           |
| `strict-random`     | ស្មើគ្នាយ៉ាងពិតប្រាកដ (គ្មានការកំណត់ទម្ងន់តាមគុណភាព)                           |
| `auto`              | ប្រើការដាក់ពិន្ទុតាមកត្តា 16 (`autoCombo/`)                                    |
| `lkgp`              | ជ្រើសរើស provider ដែលដឹងថាដំណើរការល្អចុងក្រោយមុន                               |
| `context-optimized` | ល្អបំផុតសម្រាប់សំណើដែលមាន context វែង                                          |
| `fusion`            | បញ្ជូនទៅកាន់ក្រុមមួយក្នុងពេលដំណាលគ្នា បន្ទាប់មកសំយោគតាមរយៈ judge (`fusion.ts`) |

### base.ts (1170 LOC)

**executor អរូបី** ដែល executors ទាំង 101 ពង្រីកបន្តពីវា។ វាមាន៖

- `buildUrl()` — ការសាងសង់ URL លំនាំដើម (subclasses ធ្វើ override សម្រាប់ការកំណត់តាមបំណង)
- `buildHeaders()` — headers លំនាំដើម (auth, content-type)
- `transformRequest()` — បញ្ជូនឆ្លងកាត់តាមលំនាំដើម
- `execute()` — HTTP loop ចម្បងដែលមាន retry/backoff/breaker

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // គ្រប់គ្រង providers ទាំងអស់ដែលត្រូវគ្នាជាមួយ OpenAI/Anthropic
  // Providers ចុះបញ្ជី configurations (URL, auth, headers) ប៉ុន្តែប្រើតក្កវិជ្ជា executor រួមគ្នា
}
```

ឥរិយាបថជាក់លាក់របស់ provider (auth headers, base URL, version headers) ត្រូវបានកំណត់រចនាសម្ព័ន្ធតាមរយៈ provider registry មិនមែនតាមរយៈ executor classes ដាច់ដោយឡែកទេ។

````

---

## សេវាកម្ម (117 ម៉ូឌុល)

សេវាកម្មគឺជា **ម៉ូឌុលដែលផ្តោតលើគោលបំណងតែមួយ** ដែល handlers ផ្គុំចូលគ្នា។ ប្រភេទធំៗរួមមាន៖

### ការកំណត់ផ្លូវ & Combo

- `combo.ts` — ចំណុចចូលសម្រាប់សំណើដែលកំណត់ផ្លូវតាម combo
- `services/autoCombo/` — ការដាក់ពិន្ទុដោយផ្អែកលើ 16 កត្តា និងយុទ្ធសាស្ត្រកំណត់ផ្លូវដោយស្វ័យប្រវត្តិចំនួន 8
- `wildcardRouter.ts` — ផ្គូផ្គងផ្លូវ wildcard (`gpt-*`)
- `modelFamilyFallback.ts` — fallback ក្នុង family របស់ T5

### ការកម្រិតអត្រា & កូតា

- `rateLimitManager.ts` — token bucket សម្រាប់ key+provider នីមួយៗ
- `usage.ts` — ការកត់ត្រាការប្រើប្រាស់
- `quotaCache.ts` — snapshot កូតាក្នុង memory

### គណនី & Token

- `tokenRefresh.ts` — refresh OAuth នៅពេលទទួល 401
- `accountFallback.ts` — ប្តូរទៅគណនីជំនួស
- `sessionManager.ts` — ស្ថានភាព session ច្រើនជុំ

### ភាពឆ្លាតវៃ

- `intentClassifier.ts` — ចាត់ថ្នាក់ចេតនារបស់សំណើ
- `taskAwareRouter.ts` — កំណត់ផ្លូវតាមប្រភេទកិច្ចការ
- `thinkingBudget.ts` — បែងចែក thinking tokens
- `contextManager.ts` — បញ្ចូលបរិបទនៃការកំណត់ផ្លូវ

### ភាពធន់

- `resilience.ts` — សម្របសម្រួល retry, backoff និង breaker
- `emergencyFallback.ts` — fallback ជាជម្រើសចុងក្រោយ
- `modelDeprecation.ts` — កំណត់ផ្លូវដោយស្វ័យប្រវត្តិទៅម៉ូដែលបន្តវេន

### ស្ថានភាព

- `signatureCache.ts` — លុបធាតុស្ទួនតាម signature របស់សំណើ
- `volumeDetector.ts` — កាត់បន្ថយបន្ទុក
- `contextHandoff.ts` — serialization របស់ session

### ការបង្ហាប់

- `compression/` (ថតរង) — pipeline បង្ហាប់ពេញលេញ
- ឯកសារចំនួន 39 ដែលគ្របដណ្តប់លើ engines, rule packs និង adapters

### ជំនាញ

- (បានរៀបរាប់ក្នុង [SKILLS.md](./SKILLS.md))

### Memory

- (បានរៀបរាប់ក្នុង [MEMORY.md](./MEMORY.md))

---

## Executors (75+ ឯកសារ)

មួយឯកសារសម្រាប់ provider នីមួយៗ។ ពួកវាទាំងអស់ពង្រីក `BaseExecutor` ហើយ override តែផ្នែកដែលខុសគ្នាប៉ុណ្ណោះ។

### លំនាំទូទៅ

Providers ត្រូវបានដោះស្រាយតាមរយៈ `getExecutor(providerId)` ដែលត្រឡប់ executor ដែលបានកំណត់រចនាសម្ព័ន្ធ។ Providers ដែល compatible ជាមួយ OpenAI/Anthropic ប្រើ `DefaultExecutor` (`executors/default.ts`)។ ឥរិយាបថជាក់លាក់របស់ provider (base URL, auth headers, API version) ត្រូវបានកំណត់រចនាសម្ព័ន្ធក្នុង `open-sse/config/providers/` ខណៈដែលការបំប្លែង request body ត្រូវបានដោះស្រាយក្នុង `open-sse/translator/`។

**URL ផ្ទាល់ខ្លួន** ត្រូវបានកំណត់តាមរយៈការកំណត់រចនាសម្ព័ន្ធ provider៖

```ts
// ការកំណត់រចនាសម្ព័ន្ធ provider ក្នុង open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Auth ផ្ទាល់ខ្លួន** ត្រូវបានដោះស្រាយតាមរយៈការកំណត់រចនាសម្ព័ន្ធ auth របស់ provider registry (API key, OAuth, header profiles)។

ការបំប្លែង **request body ផ្ទាល់ខ្លួន** (ឧ. Anthropic បំបែក `system` ចេញពី `messages`) ត្រូវបានចុះឈ្មោះសម្រាប់ provider នីមួយៗក្នុង `open-sse/translator/`។

````

### Executor Factory

`executors/index.ts` export `getExecutor(providerId)`៖

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

ការដោះស្រាយឆ្លងកាត់ `ExecutorRegistry` (`executors/registry.ts`)៖ specialized executor នីមួយៗត្រូវបានប្រកាសក្នុងតារាងដែលភ្ជាប់មកជាមួយរបស់ `executors/index.ts` និងចុះឈ្មោះតាមរយៈ `registerExecutor(alias, instance)` នៅពេលផ្ទុកម៉ូឌុល។ `getExecutor()` ពិនិត្យមើល registry ហើយ fallback ទៅ `DefaultExecutor` ដែលបាន memoize សម្រាប់ provider ណាមួយដែលគ្មាន specialized entry។ ការផ្គូផ្គង alias → executor ពេញលេញត្រូវបានកំណត់លក្ខណៈដោយ golden test `tests/unit/executor-map-golden.test.ts`។

---

## កម្មវិធីបកប្រែ

បកប្រែរវាង **ទម្រង់ 3**៖ OpenAI, Anthropic, Gemini រួមទាំង Responses API ថ្មីផងដែរ។

### ពេលដែលការបកប្រែកើតឡើង

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

ការបកប្រែទូទៅ៖

- `OpenAI → Anthropic`៖ វាល `system` ដាច់ដោយឡែក និង header `x-api-key`
- `OpenAI → Gemini`៖ ប្រើ `contents` ជំនួសឱ្យ `messages` និង `systemInstruction`
- `OpenAI → Responses API`៖ array `input` និង state `previous_response_id`

### ករណីពិសេសដែលបានដោះស្រាយ

- role `developer` → `system` សម្រាប់ប្រព័ន្ធដែលមិនមែនជា OpenAI
- role `system` → បញ្ចូលជាមួយសាររបស់អ្នកប្រើដំបូងសម្រាប់ GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` របស់ Gemini
- `tools` → ទម្រង់ឧបករណ៍ជាក់លាក់តាម provider
- ប៉ារ៉ាម៉ែត្រសម្រាប់ការគិត (o1, Claude) → សមមូលជាក់លាក់តាម provider

---

## MCP Server

`open-sse/mcp-server/` អនុវត្ត server សម្រាប់ **Model Context Protocol**៖

- **ឧបករណ៍ 110** (ការគ្រប់គ្រង provider, combos, memory, cache, compression, proxy, skills, gamification, plugins, Notion, Obsidian និង corpus ក្នុងមូលដ្ឋាន)
- **មធ្យោបាយបញ្ជូន 3**៖ stdio, SSE, Streamable HTTP
- **scope 33** សម្រាប់ការផ្តល់សិទ្ធិកម្រិតលម្អិត

### ការចុះបញ្ជីឧបករណ៍

ឧបករណ៍ត្រូវបានចុះបញ្ជីជាឯកសារឯករាជ្យក្នុង `open-sse/mcp-server/tools/` ដោយឯកសារនីមួយៗ export ឈ្មោះ schema, handler និង scope៖

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### មធ្យោបាយបញ្ជូន

```ts
// stdio (ការប្រើប្រាស់ CLI)
startMcpStdio(server);

// SSE (ការផ្សាយជាបន្តបន្ទាប់ផ្អែកលើ HTTP)
startMcpSse(server, port);

// Streamable HTTP (MCP ទំនើប)
startMcpStreamable(server, port);
```

### ការផ្តល់សិទ្ធិ

រាល់ការហៅឧបករណ៍ទាំងអស់ឆ្លងកាត់ការត្រួតពិនិត្យ scope (`open-sse/mcp-server/auth/`)៖

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## កម្មវិធីបំប្លែង

`open-sse/transformer/` បំប្លែងរវាងទម្រង់ **Chat Completions** និង **Responses API**។

### ហេតុអ្វីត្រូវការកម្មវិធីបំប្លែងដាច់ដោយឡែក?

Responses API គឺជាទម្រង់ថ្មីរបស់ OpenAI ដែលមាន **ការសន្ទនារក្សាទុក state** (`previous_response_id`)។ នៅពេល client ផ្ញើសំណើ Responses មក OmniRoute នឹង៖

1. បំប្លែង Responses → Chat Completions នៅខាងក្នុង
2. ផ្ញើទៅ provider (provider ណាមួយដែលគាំទ្រ Chat Completions)
3. បំប្លែង response ត្រឡប់ទៅជាទម្រង់ Responses
4. ផ្សាយ response ដែលបានបំប្លែងទៅ client ជាបន្តបន្ទាប់

កម្មវិធីបំប្លែង (`transformer/responsesTransformer.ts`) ផ្តល់ជូន៖

```ts
createResponsesApiTransformStream(): TransformStream
```

វាដោះស្រាយ៖

- event `response.output_item.added`
- event `response.output_text.delta`
- event `response.completed`
- ការផ្គូផ្គងការហៅឧបករណ៍ (`function_call` ↔ `tool_calls`)

---

## ការកំណត់រចនាសម្ព័ន្ធ

`open-sse/config/` ផ្ទុក layer សម្រាប់ការកំណត់រចនាសម្ព័ន្ធ៖

| ឯកសារ                            | គោលបំណង                                               |
| -------------------------------- | ----------------------------------------------------- |
| `providerRegistry.ts`            | បញ្ជីចុះឈ្មោះ chat-model លើកាតាឡុក provider ចំនួន 352 |
| `providerModels.ts`              | ឈ្មោះក្លែងក្លាយរបស់ model និងការផ្គូផ្គងទម្រង់        |
| `constants.ts`                   | ពេលផុតកំណត់ ដែនកំណត់ និង status code                  |
| `defaultThinkingSignature.ts`    | thinking signature លំនាំដើមរបស់ Claude                |
| `modelStrip.ts` (ក្នុង services) | ការដកវាលចេញតាម provider                               |

### Schema នៃបញ្ជីចុះឈ្មោះ Provider

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

ការផ្ទៀងផ្ទាត់ Zod នៅពេល module ត្រូវបាន load ធានាថា config ទាំងអស់របស់ provider មានសុពលភាព។

---

## កំហិតផ្នែកប្រសិទ្ធភាព

ម៉ាស៊ីនកំណត់ផ្លូវមានកម្រិតថវិកាប្រសិទ្ធភាពយ៉ាងតឹងរ៉ឹង៖

| ប្រតិបត្តិការ                                                    | គោលដៅ | ការវាស់វែង                                   |
| ---------------------------------------------------------------- | ----- | -------------------------------------------- |
| ការដោះស្រាយ Combo                                                | <10ms | សម្រាប់គោលដៅ 50                              |
| ការត្រួតពិនិត្យកម្រិតអត្រា                                       | <1ms  | ធុង token ក្នុងអង្គចងចាំ                     |
| ការបម្រុងទុកតាមក្រុមម៉ូដែល                                       | <5ms  | និយមន័យក្រុមដែលបានរក្សាទុកក្នុងឃ្លាំងសម្ងាត់ |
| ការបញ្ជូនសំណើតាមផ្លូវ                                            | <2ms  | ផ្លូវដំណើរការសំខាន់                          |
| **គ្មាន I/O ដែលរារាំងនៅក្នុងផ្លូវដំណើរការសំខាន់នៃការកំណត់ផ្លូវ** | —     | ទាំងអស់ជា async                              |

---

## លំនាំដែលត្រូវជៀសវាង

❌ **ការហៅ DB បែបសមកាលកម្មនៅក្នុង `combo.ts`** — គណនាជាមុន និងរក្សាទុកក្នុងឃ្លាំងសម្ងាត់
❌ **តក្កវិជ្ជាព្យាយាមឡើងវិញនៅក្នុង handlers** — ប្រើ `retry()` ពីសេវាធន់ទ្រាំ
❌ **ការចូលប្រើការកំណត់រចនាសម្ព័ន្ធ provider ដោយផ្ទាល់** — ប្រើ getters របស់ `providerRegistry`
❌ **ខ្សែសង្វាក់បម្រុងទុកដែលសរសេរកូដថេរ** — កំណត់នៅក្នុង `modelFamilyFallback.ts`
❌ **ការកែប្រែ state ឆ្លងកាត់សំណើដែលដំណើរការដំណាលគ្នា** — ប្រើតែ context ដែលមានវិសាលភាពតាមសំណើ

---

## ការបន្ថែមសមាសភាគថ្មី

### ការបន្ថែមសេវាថ្មី

1. បង្កើត `open-sse/services/[serviceName].ts` ដែលមានការទទួលខុសត្រូវផ្តោតច្បាស់លាស់
2. Export អនុគមន៍ handler ចម្បង និង constants ណាមួយ
3. បន្ថែមការធ្វើតេស្តឯកតានៅក្នុង `tests/unit/services/[serviceName].test.mjs`
4. បញ្ចូលទៅក្នុងខ្សែដំណើរការសំណើនៅក្នុង `handlers/chatCore.ts` (ប្រសិនបើពាក់ព័ន្ធនឹងការកំណត់ផ្លូវ)
5. ធ្វើបច្ចុប្បន្នភាពតក្កវិជ្ជាកំណត់ផ្លូវនៅក្នុង `combo.ts` ប្រសិនបើសេវាប៉ះពាល់ដល់ការជ្រើសរើសគោលដៅ
6. ចងក្រងជាឯកសារនៅក្នុងឯកសារនេះ

### ការបន្ថែម Executor ថ្មី

1. បង្កើត `open-sse/executors/[provider].ts` ដែលពង្រីកពី `BaseExecutor`
2. ចុះឈ្មោះនៅក្នុង `config/providerRegistry.ts`
3. បន្ថែមទៅ factory របស់ `executors/index.ts`
4. បន្ថែមការធ្វើតេស្តឯកតាសម្រាប់ executor
5. ចងក្រងជាឯកសារនៅក្នុង `docs/architecture/ARCHITECTURE.md`

### ការបន្ថែមឧបករណ៍ MCP ថ្មី

1. បង្កើត ឬធ្វើបច្ចុប្បន្នភាព `open-sse/mcp-server/tools/[category]Tools.ts`
2. កំណត់ Zod schema សម្រាប់ inputs
3. ចុះឈ្មោះឧបករណ៍នៅក្នុង `mcp-server/index.ts`
4. បន្ថែមទៅម៉ាទ្រីសវិសាលភាពនៅក្នុង `mcp-server/auth/`
5. បន្ថែមការធ្វើតេស្តឯកតា

---

## សូមមើលផងដែរ

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — ស្ថាបត្យកម្មកម្រិតខ្ពស់
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — ឯកសារយោងវិស្វកម្ម
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — ព័ត៌មានលម្អិតតាមថតនីមួយៗ
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — ការដាក់ពិន្ទុ 16 កត្តា
- [MCP-SERVER.md](./MCP-SERVER.md) — ម៉ាស៊ីនមេ MCP
- [A2A-SERVER.md](./A2A-SERVER.md) — ម៉ាស៊ីនមេ A2A
- កូដប្រភព៖ `open-sse/` (ឯកសារ 400+, ~143K LOC)
