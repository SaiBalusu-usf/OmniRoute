# open-sse Architecture (ไทย)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **สรุปสั้นๆ**: `open-sse/` คือเอนจินสตรีมมิงหลักที่ขับเคลื่อนคำขอ LLM ทุกคำขอใน OmniRoute โดยมีไฟล์ประมาณ 1,400 ไฟล์ที่ใช้สร้างไปป์ไลน์คำขอ ตัวดำเนินการ บริการ เซิร์ฟเวอร์ MCP และเลเยอร์การแปล คู่มือนี้อธิบายว่าส่วนต่างๆ ทำงานร่วมกันอย่างไร

**ซอร์ส:** `open-sse/` (แพ็กเกจใน workspace, ไฟล์ `.ts` ที่ไม่ใช่ไฟล์ทดสอบประมาณ 1,440 ไฟล์)

---

## เหตุใดจึงต้องมีแพ็กเกจ Workspace แยกต่างหาก?

`open-sse/` เป็น **workspace แบบสแตนด์อโลน** ใน monorepo ของ OmniRoute ด้วยเหตุผลหลายประการ:

1. **นำกลับมาใช้ใหม่ได้** — `open-sse` เผยแพร่เป็น `@omniroute/open-sse` บน npm ดังนั้นโปรเจกต์อื่นจึงสามารถใช้งานได้อย่างอิสระ
2. **ขอบเขตที่ชัดเจน** — เอนจินสตรีมมิงแยกออกจากเลเยอร์ UI/DB ที่เจาะจงสำหรับ OmniRoute
3. **ประสิทธิภาพ** — เอนจินไม่มี dependency ของ Next.js ทำให้เริ่มต้นแบบ cold start ได้เร็วขึ้นในบริบทของ CLI/serverless
4. **การกำหนดเวอร์ชัน** — `open-sse` สามารถออกรุ่นตามรอบของตนเองได้

```json
// package.json
"workspaces": ["open-sse"]
```

---

## โครงสร้างระดับบนสุด

```
open-sse/
├── index.ts              # จุดเริ่มต้นสาธารณะ
├── types.d.ts            # การส่งออกชนิดข้อมูลสาธารณะ
├── package.json          # @omniroute/open-sse
├── config/               # การกำหนดค่าผู้ให้บริการ ค่าคงที่ รีจิสทรี
├── executors/            # ตัวดำเนินการ HTTP แยกตามผู้ให้บริการ (67 + base.ts/index.ts)
├── handlers/             # ตัวจัดการคำขอ (chatCore, responses เป็นต้น)
├── lib/                  # ยูทิลิตีภายใน
├── mcp-server/           # เซิร์ฟเวอร์ Model Context Protocol
├── services/             # โมดูลบริการประมาณ 298 โมดูล
├── transformer/          # ตัวแปลงรูปแบบ Responses API
├── translator/           # การแปลรูปแบบ (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # ยูทิลิตีที่ใช้ร่วมกัน (การบันทึกล็อก ข้อผิดพลาด สตรีม เป็นต้น)
```

### จำนวนโมดูล

| ไดเรกทอรี | ไฟล์ | วัตถุประสงค์ |
| `executors/` | 167 | ตัวดำเนินการ HTTP แยกตามผู้ให้บริการ (รวมเป็นหนึ่งผ่านแฟกทอรี DefaultExecutor) |
| `handlers/` | 157 | จุดเริ่มต้นคำขอ (chatCore, responses, embeddings) |
| `services/` | ~536 | การกำหนดเส้นทาง การแคช การจำกัดอัตรา การรีเฟรช เป็นต้น |
| `translator/` | 56 | การแปลงรูปแบบ (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | เครื่องมือและ transport ของ MCP |
| `utils/` | ~108 | ยูทิลิตีที่ครอบคลุมหลายส่วน (การบันทึกล็อก ข้อผิดพลาด สตรีม) |
| `config/` | ~339 | การกำหนดค่าผู้ให้บริการ ค่าคงที่ รีจิสทรี |

---

## ไปป์ไลน์คำขอ

คำขอ LLM ทุกคำขอไหลผ่าน **ไปป์ไลน์ 5 ขั้นตอน**:

```
                              ┌──────────────┐
   คำขอ HTTP                  │  1. กำหนดเส้นทาง │   การแก้ไข combo, การเลือกโมเดล
   (เส้นทาง Next.js)          └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. แปล      │   การแปลงรูปแบบ (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. ดำเนินการ │   ตัวดำเนินการของผู้ให้บริการ, HTTP, การลองใหม่, เบรกเกอร์
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. สตรีม    │   การแปลง SSE, backpressure
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. บันทึก   │   การติดตามการใช้งาน, บันทึกการเรียก, การจำแนกข้อผิดพลาด
                              └──────┬───────┘
                                     │
                                     ▼
                              การตอบกลับ HTTP (SSE หรือ JSON)
```

### ขั้นตอนที่ 1: กำหนดเส้นทาง (services/combo.ts)

**จุดเริ่มต้น**: `handleComboChat()` ใน `services/combo.ts`

แก้ไขคำขอให้เป็นทูเพิล `(provider, model, account, credentials)` ที่เป็นรูปธรรม:

- ค้นหา combo ตาม ID (หรือสร้าง combo เสมือนสำหรับโมเดล `auto/*`)
- ใช้กลยุทธ์การกำหนดเส้นทาง (ลำดับความสำคัญ แบบถ่วงน้ำหนัก round-robin เป็นต้น)
- กรองผู้ให้บริการที่ไม่พร้อมใช้งานออก (circuit breaker)
- เลือกเป้าหมายถัดไปที่ใช้งานได้

สำหรับโมเดล `auto/*` ขั้นตอนนี้ยังดำเนินการดังต่อไปนี้:

- เรียกใช้อัลกอริทึม **การให้คะแนน 16 ปัจจัย** (`services/autoCombo/`)
- เลือกคู่ `provider+model` ตามสถานะการทำงาน ต้นทุน เวลาแฝง เป็นต้น

### ขั้นตอนที่ 2: แปล (translator/)

หากรูปแบบต้นทาง (เช่น OpenAI) แตกต่างจากรูปแบบเป้าหมาย (เช่น Claude) คำขอจะถูก **แปล**:

- พรอมต์ระบบ → ข้อความระบบ
- นิยามเครื่องมือ → รูปแบบเครื่องมือเฉพาะของผู้ให้บริการ
- พารามิเตอร์การให้เหตุผล/การคิด → ค่าที่เทียบเท่าเฉพาะของผู้ให้บริการ
- การปรับบทบาทข้อความให้เป็นมาตรฐาน (`developer` → `system` สำหรับระบบที่ไม่ใช่ OpenAI)

`translator/index.ts` เปิดเผย:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### ขั้นตอนที่ 3: ดำเนินการ (executors/)

**จุดเริ่มต้น**: `getExecutor(providerId).execute(request, options)`

ผู้ให้บริการทั้งหมดใช้ `DefaultExecutor` (`executors/default.ts`) ผ่านกลไก fallback ของแฟกทอรี `getExecutor()` ตัวดำเนินการจะ:

- สร้าง URL ต้นทาง (`buildUrl()`)
- เพิ่มส่วนหัวเฉพาะของผู้ให้บริการ (`buildHeaders()`)
- แปลงเนื้อหาคำขอ (`transformRequest()`)
- ส่งคำขอ HTTP พร้อมการลองใหม่ + exponential backoff
- จัดการการรีเฟรชการยืนยันตัวตนหากจำเป็น (ผู้ให้บริการ OAuth)

ตัวดำเนินการทั้งหมดสืบทอดจาก `BaseExecutor` (`executors/base.ts`, 1170 LOC) ซึ่งมีความสามารถดังต่อไปนี้:

- ตรรกะการลองใหม่ที่ใช้ร่วมกัน
- การผสานรวมพร็อกซี
- การผสานรวม circuit breaker
- hook สำหรับบันทึกการใช้งาน

### ขั้นตอนที่ 4: สตรีม (utils/stream.ts)

สำหรับการตอบกลับแบบสตรีม ตัวดำเนินการจะคืนค่า **ReadableStream** ตัวจัดการจะ:

- ส่งผ่านตัวแปลง SSE (`createSSETransformStreamWithLogger`)
- ใช้ heartbeat ping เพื่อตรวจหาการเชื่อมต่อที่ขาดหาย
- จัดการการตัดการเชื่อมต่อของไคลเอนต์อย่างเหมาะสม (`pipeWithDisconnect`)
- แปลง SSE → JSON สำหรับไคลเอนต์ที่ไม่ใช้สตรีม

สำหรับการตอบกลับที่ไม่ใช่แบบสตรีม ตัวดำเนินการจะคืนค่าออบเจ็กต์ JSON ที่ผ่านการพาร์สแล้ว ซึ่งจะถูกส่งต่อโดยไม่มีการเปลี่ยนแปลง

### ขั้นตอนที่ 5: บันทึก (services/usage.ts)

หลังจากได้รับการตอบกลับ (ไม่ว่าจะสำเร็จหรือล้มเหลว) ระบบจะบันทึกข้อมูลการใช้งาน:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` จากการตอบกลับ
- `cost_usd` ที่คำนวณจากข้อมูลราคา
- `latency_ms`, `status`, `error_class` หากล้มเหลว
- จัดเก็บถาวรในตาราง `usage_history`

อาร์ติแฟกต์ของบันทึกการเรียกใช้ (หากเปิดใช้งาน) จะถูกเขียนไปยัง `${DATA_DIR}/call_logs/`

---

## เจาะลึกไฟล์สำคัญ

### chatCore.ts (5977 บรรทัด)

**ตัวจัดการคำขอหลัก** แม้ไฟล์จะมีขนาดใหญ่ แต่ก็มีโครงสร้างที่ชัดเจน:

```ts
// โครงสร้างจำลองของ chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. การยืนยันตัวตน + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. การตรวจสอบความถูกต้องของเนื้อหาคำขอ
  const body = await parseRequestBody(request);

  // 3. การตรวจหารูปแบบ + การแปลง
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. การกำหนดเส้นทางแบบคอมโบ
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // ดำเนินการต่อไปยังเป้าหมายถัดไป
    }
  }

  // 5. ทางเลือกสำรองฉุกเฉิน
  return await emergencyFallback(body);
}
```

แม้จะเป็นฟังก์ชันขนาดใหญ่เพียงฟังก์ชันเดียว แต่ก็จัดระเบียบเป็น **ส่วนที่มีความคิดเห็นกำกับ** ซึ่งสอดคล้องกับไปป์ไลน์ 5 ขั้นตอน

### combo.ts (4456 LOC)

**เอนจินกำหนดเส้นทาง** ซึ่งแปลงคอมโบให้เป็นรายการเป้าหมายตามลำดับ

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

รองรับ **กลยุทธ์การกำหนดเส้นทาง 19 แบบ** (ดู `src/shared/constants/routingStrategies.ts`):

| กลยุทธ์             | ลักษณะการทำงาน                                                                  |
| ------------------- | ------------------------------------------------------------------------------- |
| `priority`          | รายการที่เรียงโดยให้เป้าหมายแรกรับลำดับความสำคัญสูงสุด                          |
| `weighted`          | เลือกตามความน่าจะเป็นโดยอิงน้ำหนักของแต่ละเป้าหมาย                              |
| `round-robin`       | วนเลือกเป้าหมายตามลำดับ                                                         |
| `context-relay`     | ส่งต่อบริบทระหว่างเป้าหมาย                                                      |
| `fill-first`        | ใช้โควตาให้เต็มก่อนย้ายไปยังเป้าหมายถัดไป                                       |
| `p2c`               | เลือกหนึ่งจากสองตัวเลือก                                                        |
| `random`            | สุ่มแบบสม่ำเสมอ                                                                 |
| `least-used`        | เลือกเป้าหมายที่ถูกใช้งานน้อยที่สุดในช่วงที่ผ่านมา                              |
| `cost-optimized`    | เลือกเป้าหมายที่พร้อมใช้งานและมีราคาถูกที่สุดก่อน                               |
| `reset-aware`       | คำนึงถึงช่วงเวลารีเซ็ตของผู้ให้บริการ                                           |
| `reset-window`      | กำหนดเส้นทางตามช่วงเวลารีเซ็ต                                                   |
| `headroom`          | เลือกเป้าหมายที่มีโควตาคงเหลือมากที่สุดก่อน                                     |
| `strict-random`     | สุ่มแบบสม่ำเสมออย่างแท้จริง (ไม่มีการถ่วงน้ำหนักตามคุณภาพ)                      |
| `auto`              | ใช้การให้คะแนนจาก 16 ปัจจัย (`autoCombo/`)                                      |
| `lkgp`              | เลือกผู้ให้บริการล่าสุดที่ทราบว่าใช้งานได้เป็นอันดับแรก                         |
| `context-optimized` | เหมาะที่สุดสำหรับคำขอที่มีบริบทยาว                                              |
| `fusion`            | กระจายคำขอไปยังกลุ่มเป้าหมายแบบขนาน แล้วสังเคราะห์ผลผ่านตัวตัดสิน (`fusion.ts`) |

### base.ts (1170 LOC)

**ตัวดำเนินการเชิงนามธรรม** ที่ตัวดำเนินการทั้ง 101 รายการสืบทอด โดยประกอบด้วย:

- `buildUrl()` — การสร้าง URL เริ่มต้น (คลาสย่อยสามารถเขียนทับเพื่อปรับแต่งได้)
- `buildHeaders()` — ส่วนหัวเริ่มต้น (การยืนยันตัวตน, content-type)
- `transformRequest()` — ส่งผ่านโดยไม่มีการเปลี่ยนแปลงตามค่าเริ่มต้น
- `execute()` — ลูป HTTP หลักพร้อมการลองใหม่/การหน่วงเวลาแบบทวีคูณ/เซอร์กิตเบรกเกอร์

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // รองรับผู้ให้บริการทั้งหมดที่เข้ากันได้กับ OpenAI/Anthropic
  // ผู้ให้บริการลงทะเบียนการกำหนดค่า (URL, การยืนยันตัวตน, ส่วนหัว) แต่ใช้ตรรกะตัวดำเนินการร่วมกัน
}
```

พฤติกรรมเฉพาะของผู้ให้บริการ (ส่วนหัวการยืนยันตัวตน, URL ฐาน, ส่วนหัวเวอร์ชัน) จะได้รับการกำหนดค่าผ่านรีจิสทรีผู้ให้บริการ ไม่ใช่คลาสตัวดำเนินการแยกต่างหาก

````

---

## บริการ (117 โมดูล)

บริการคือ **โมดูลที่มุ่งเน้นวัตถุประสงค์เดียว** ซึ่งแฮนด์เลอร์นำมาประกอบใช้งาน หมวดหมู่หลักได้แก่:

### การกำหนดเส้นทางและคอมโบ

- `combo.ts` — จุดเริ่มต้นสำหรับคำขอที่กำหนดเส้นทางแบบคอมโบ
- `services/autoCombo/` — การให้คะแนนด้วยปัจจัย 16 ข้อ และกลยุทธ์การกำหนดเส้นทางอัตโนมัติ 8 แบบ
- `wildcardRouter.ts` — จับคู่เส้นทางแบบไวลด์การ์ด (`gpt-*`)
- `modelFamilyFallback.ts` — การสำรองภายในตระกูล T5

### การจำกัดอัตราและโควตา

- `rateLimitManager.ts` — ถังโทเค็นต่อคีย์+ผู้ให้บริการ
- `usage.ts` — การบันทึกการใช้งาน
- `quotaCache.ts` — สแนปช็อตโควตาในหน่วยความจำ

### บัญชีและโทเค็น

- `tokenRefresh.ts` — รีเฟรช OAuth เมื่อได้รับ 401
- `accountFallback.ts` — สลับไปใช้บัญชีสำรอง
- `sessionManager.ts` — สถานะเซสชันแบบหลายรอบการสนทนา

### ความชาญฉลาด

- `intentClassifier.ts` — จำแนกเจตนาของคำขอ
- `taskAwareRouter.ts` — กำหนดเส้นทางตามประเภทงาน
- `thinkingBudget.ts` — จัดสรรโทเค็นสำหรับการคิด
- `contextManager.ts` — แทรกบริบทการกำหนดเส้นทาง

### ความทนทาน

- `resilience.ts` — ควบคุมการลองใหม่ การหน่วงเวลาแบบทวีคูณ และเซอร์กิตเบรกเกอร์
- `emergencyFallback.ts` — ตัวสำรองสุดท้าย
- `modelDeprecation.ts` — กำหนดเส้นทางไปยังโมเดลตัวถัดไปโดยอัตโนมัติ

### สถานะ

- `signatureCache.ts` — ขจัดรายการซ้ำตามลายเซ็นของคำขอ
- `volumeDetector.ts` — ลดภาระงาน
- `contextHandoff.ts` — แปลงเซสชันให้อยู่ในรูปแบบที่จัดเก็บหรือส่งต่อได้

### การบีบอัด

- `compression/` (ไดเรกทอรีย่อย) — ไปป์ไลน์การบีบอัดแบบครบวงจร
- 39 ไฟล์ ครอบคลุมเอนจิน ชุดกฎ และอะแดปเตอร์

### ทักษะ

- (อธิบายไว้ใน [SKILLS.md](./SKILLS.md))

### หน่วยความจำ

- (อธิบายไว้ใน [MEMORY.md](./MEMORY.md))

---

## ตัวดำเนินการ (มากกว่า 75 ไฟล์)

หนึ่งไฟล์ต่อผู้ให้บริการ ตัวดำเนินการทั้งหมดสืบทอดจาก `BaseExecutor` และเขียนทับเฉพาะส่วนที่แตกต่างกัน

### รูปแบบทั่วไป

ผู้ให้บริการจะถูกจับคู่ผ่าน `getExecutor(providerId)` ซึ่งส่งคืนตัวดำเนินการที่กำหนดค่าไว้ ผู้ให้บริการที่เข้ากันได้กับ OpenAI/Anthropic จะใช้ `DefaultExecutor` (`executors/default.ts`) พฤติกรรมเฉพาะของผู้ให้บริการ (URL ฐาน เฮดเดอร์การยืนยันตัวตน เวอร์ชัน API) จะได้รับการกำหนดค่าใน `open-sse/config/providers/` ส่วนการแปลงเนื้อหาคำขอจะได้รับการจัดการใน `open-sse/translator/`

**URL แบบกำหนดเอง** ตั้งค่าผ่านการกำหนดค่าผู้ให้บริการ:

```ts
// การกำหนดค่าผู้ให้บริการใน open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**การยืนยันตัวตนแบบกำหนดเอง** ได้รับการจัดการผ่านการกำหนดค่าการยืนยันตัวตนของรีจิสทรีผู้ให้บริการ (คีย์ API, OAuth, โปรไฟล์เฮดเดอร์)

การแปลง **เนื้อหาคำขอแบบกำหนดเอง** (เช่น Anthropic แยก `system` ออกจาก `messages`) จะได้รับการลงทะเบียนแยกตามผู้ให้บริการใน `open-sse/translator/`

````

### แฟกทอรีตัวดำเนินการ

`executors/index.ts` ส่งออก `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

การจับคู่จะดำเนินการผ่าน `ExecutorRegistry` (`executors/registry.ts`): ตัวดำเนินการเฉพาะทางทุกตัวถูกประกาศไว้ในตารางในตัวของ `executors/index.ts` และลงทะเบียนผ่าน `registerExecutor(alias, instance)` ขณะโหลดโมดูล โดย `getExecutor()` จะตรวจสอบรีจิสทรีและย้อนกลับไปใช้ `DefaultExecutor` ที่มีการจดจำผลลัพธ์สำหรับผู้ให้บริการที่ไม่มีรายการเฉพาะ การจับคู่ alias → executor ทั้งหมดได้รับการกำหนดลักษณะไว้โดยการทดสอบโกลเดน `tests/unit/executor-map-golden.test.ts`

---

## ตัวแปล

แปลระหว่าง **3 รูปแบบ** ได้แก่ OpenAI, Anthropic, Gemini รวมถึง Responses API ใหม่

### การแปลเกิดขึ้นเมื่อใด

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

การแปลที่พบบ่อย:

- `OpenAI → Anthropic`: แยกฟิลด์ `system` และใช้ส่วนหัว `x-api-key`
- `OpenAI → Gemini`: ใช้ `contents` แทน `messages` และใช้ `systemInstruction`
- `OpenAI → Responses API`: ใช้อาร์เรย์ `input` และสถานะ `previous_response_id`

### กรณีพิเศษที่รองรับ

- บทบาท `developer` → `system` สำหรับผู้ให้บริการที่ไม่ใช่ OpenAI
- บทบาท `system` → ผสานเข้ากับข้อความแรกของผู้ใช้สำหรับ GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` ของ Gemini
- `tools` → รูปแบบเครื่องมือเฉพาะของผู้ให้บริการ
- พารามิเตอร์การคิด (o1, Claude) → ค่าเทียบเท่าเฉพาะของผู้ให้บริการ

---

## เซิร์ฟเวอร์ MCP

`open-sse/mcp-server/` ใช้งานเซิร์ฟเวอร์ **Model Context Protocol**:

- **110 เครื่องมือ** (การจัดการผู้ให้บริการ, คอมโบ, หน่วยความจำ, แคช, การบีบอัด, พร็อกซี, ทักษะ, เกมมิฟิเคชัน, ปลั๊กอิน, Notion, Obsidian, คลังข้อมูลภายในเครื่อง)
- **3 ทรานสปอร์ต**: stdio, SSE, Streamable HTTP
- **33 ขอบเขตสิทธิ์** สำหรับการให้สิทธิ์แบบละเอียด

### การลงทะเบียนเครื่องมือ

เครื่องมือจะถูกลงทะเบียนเป็นไฟล์แยกใน `open-sse/mcp-server/tools/` โดยแต่ละไฟล์จะส่งออกชื่อ สคีมา ตัวจัดการ และขอบเขตสิทธิ์:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "รับสแนปช็อตสถานะความพร้อมของระบบ",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### ทรานสปอร์ต

```ts
// stdio (การใช้งานผ่าน CLI)
startMcpStdio(server);

// SSE (การสตรีมผ่าน HTTP)
startMcpSse(server, port);

// Streamable HTTP (MCP สมัยใหม่)
startMcpStreamable(server, port);
```

### การให้สิทธิ์

การเรียกใช้เครื่องมือทุกครั้งจะผ่านการตรวจสอบขอบเขตสิทธิ์ (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("ขอบเขตสิทธิ์ไม่เพียงพอ");
}
```

---

## ตัวแปลงรูปแบบ

`open-sse/transformer/` แปลงระหว่างรูปแบบ **Chat Completions** และ **Responses API**

### เหตุใดจึงต้องมีตัวแปลงแยกต่างหาก?

Responses API เป็นรูปแบบใหม่ของ OpenAI ที่มี **การสนทนาแบบเก็บสถานะ** (`previous_response_id`) เมื่อไคลเอนต์ส่งคำขอ Responses ทาง OmniRoute จะ:

1. แปลง Responses → Chat Completions ภายในระบบ
2. ส่งไปยังผู้ให้บริการ (ผู้ให้บริการใดก็ได้ที่รองรับ Chat Completions)
3. แปลงการตอบกลับกลับเป็นรูปแบบ Responses
4. สตรีมการตอบกลับที่แปลงแล้วไปยังไคลเอนต์

ตัวแปลง (`transformer/responsesTransformer.ts`) มีรายการต่อไปนี้:

```ts
createResponsesApiTransformStream(): TransformStream
```

ซึ่งรองรับ:

- เหตุการณ์ `response.output_item.added`
- เหตุการณ์ `response.output_text.delta`
- เหตุการณ์ `response.completed`
- การแมปการเรียกใช้เครื่องมือ (`function_call` ↔ `tool_calls`)

---

## การกำหนดค่า

`open-sse/config/` จัดเก็บเลเยอร์การกำหนดค่า:

| ไฟล์                          | วัตถุประสงค์                                     |
| ----------------------------- | ------------------------------------------------ |
| `providerRegistry.ts`         | รีจิสทรีโมเดลแชตบนแค็ตตาล็อกผู้ให้บริการ 352 ราย |
| `providerModels.ts`           | ชื่อแทนของโมเดลและการแมปรูปแบบ                   |
| `constants.ts`                | ระยะหมดเวลา ขีดจำกัด และรหัสสถานะ                |
| `defaultThinkingSignature.ts` | ลายเซ็นการคิดเริ่มต้นของ Claude                  |
| `modelStrip.ts` (ใน services) | การตัดฟิลด์แยกตามผู้ให้บริการ                    |

### สคีมารีจิสทรีผู้ให้บริการ

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

การตรวจสอบความถูกต้องด้วย Zod ขณะโหลดโมดูลช่วยให้มั่นใจว่าการกำหนดค่าผู้ให้บริการทั้งหมดถูกต้อง

---

## ข้อจำกัดด้านประสิทธิภาพ

เอนจินการกำหนดเส้นทางมีงบประมาณด้านประสิทธิภาพที่เข้มงวด:

| การดำเนินการ                                        | เป้าหมาย | การวัดผล                   |
| --------------------------------------------------- | -------- | -------------------------- |
| การเลือกคอมโบ                                       | <10ms    | สำหรับ 50 เป้าหมาย         |
| การตรวจสอบขีดจำกัดอัตรา                             | <1ms     | Token bucket ในหน่วยความจำ |
| การสำรองไปยังตระกูลโมเดลอื่น                        | <5ms     | ข้อมูลตระกูลที่แคชไว้      |
| การส่งต่อคำขอตามเส้นทาง                             | <2ms     | เส้นทางการทำงานหลัก        |
| **ห้ามมี I/O แบบบล็อกในเส้นทางการกำหนดเส้นทางหลัก** | —        | เป็นแบบอะซิงโครนัสทั้งหมด  |

---

## รูปแบบที่ไม่ควรใช้

❌ **การเรียกฐานข้อมูลแบบซิงโครนัสใน `combo.ts`** — คำนวณล่วงหน้าและแคช
❌ **ตรรกะการลองใหม่ในตัวจัดการ** — ใช้ `retry()` จากบริการความยืดหยุ่น
❌ **การเข้าถึงการกำหนดค่าผู้ให้บริการโดยตรง** — ใช้ getter ของ `providerRegistry`
❌ **เชนสำรองที่ฮาร์ดโค้ดไว้** — กำหนดใน `modelFamilyFallback.ts`
❌ **การแก้ไขสถานะข้ามคำขอที่ทำงานพร้อมกัน** — ใช้เฉพาะบริบทที่มีขอบเขตระดับคำขอ

---

## การเพิ่มคอมโพเนนต์ใหม่

### การเพิ่มบริการใหม่

1. สร้าง `open-sse/services/[serviceName].ts` โดยมีหน้าที่เฉพาะเจาะจง
2. ส่งออกฟังก์ชันตัวจัดการหลักและค่าคงที่ต่าง ๆ
3. เพิ่มการทดสอบหน่วยใน `tests/unit/services/[serviceName].test.mjs`
4. ผสานรวมเข้ากับไปป์ไลน์คำขอใน `handlers/chatCore.ts` (หากเกี่ยวข้องกับการกำหนดเส้นทาง)
5. อัปเดตตรรกะการกำหนดเส้นทางใน `combo.ts` หากบริการมีผลต่อการเลือกเป้าหมาย
6. จัดทำเอกสารในไฟล์นี้

### การเพิ่มตัวดำเนินการใหม่

1. สร้าง `open-sse/executors/[provider].ts` โดยสืบทอดจาก `BaseExecutor`
2. ลงทะเบียนใน `config/providerRegistry.ts`
3. เพิ่มลงใน factory ของ `executors/index.ts`
4. เพิ่มการทดสอบหน่วยสำหรับตัวดำเนินการ
5. จัดทำเอกสารใน `docs/architecture/ARCHITECTURE.md`

### การเพิ่มเครื่องมือ MCP ใหม่

1. สร้างหรืออัปเดต `open-sse/mcp-server/tools/[category]Tools.ts`
2. กำหนดสคีมา Zod สำหรับอินพุต
3. ลงทะเบียนเครื่องมือใน `mcp-server/index.ts`
4. เพิ่มลงในเมทริกซ์ขอบเขตใน `mcp-server/auth/`
5. เพิ่มการทดสอบหน่วย

---

## ดูเพิ่มเติม

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — สถาปัตยกรรมระดับสูง
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — เอกสารอ้างอิงด้านวิศวกรรม
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — รายละเอียดของแต่ละไดเรกทอรี
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — การให้คะแนน 16 ปัจจัย
- [MCP-SERVER.md](./MCP-SERVER.md) — เซิร์ฟเวอร์ MCP
- [A2A-SERVER.md](./A2A-SERVER.md) — เซิร์ฟเวอร์ A2A
- ซอร์สโค้ด: `open-sse/` (มากกว่า 400 ไฟล์, ~143K LOC)
