# open-sse Architecture (Kiswahili)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Kwa Ufupi**: `open-sse/` ni injini kuu ya utiririshaji inayowezesha kila ombi la LLM katika OmniRoute. Ina takribani faili 1,400 zinazotekeleza mchakato wa maombi, vitekelezaji, huduma, seva ya MCP, na safu ya utafsiri. Mwongozo huu unaeleza jinsi vipande hivi vinavyoungana.

**Chanzo:** `open-sse/` (kifurushi cha workspace, takribani faili 1,440 zisizo za majaribio za `.ts`)

---

## Kwa Nini Kuwe na Kifurushi Tofauti cha Workspace?

`open-sse/` ni **workspace inayojitegemea** katika monorepo ya OmniRoute kwa sababu kadhaa:

1. **Uwezo wa kutumika tena** — `open-sse` huchapishwa kama `@omniroute/open-sse` kwenye npm, hivyo miradi mingine inaweza kuitumia kivyake
2. **Mipaka iliyo wazi** — injini ya utiririshaji imetenganishwa na safu ya UI/DB inayohusika mahususi na OmniRoute
3. **Utendaji** — injini haina utegemezi wowote wa Next.js, jambo linalowezesha uanzishaji wa awali ulio wa haraka zaidi katika mazingira ya CLI/serverless
4. **Udhibiti wa matoleo** — `open-sse` inaweza kutoa matoleo kwa ratiba yake yenyewe

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Muundo wa Ngazi ya Juu

```
open-sse/
├── index.ts              # Sehemu ya kuingilia ya umma
├── types.d.ts            # Hamisho za aina za umma
├── package.json          # @omniroute/open-sse
├── config/               # Mipangilio ya watoa huduma, thamani zisizobadilika, sajili
├── executors/            # Vitekelezaji vya HTTP kwa kila mtoa huduma (67 + base.ts/index.ts)
├── handlers/             # Vishughulikiaji vya maombi (chatCore, responses, n.k.)
├── lib/                  # Zana za ndani
├── mcp-server/           # Seva ya Model Context Protocol
├── services/             # Takribani moduli 298 za huduma
├── transformer/          # Kigeuzi cha umbizo la Responses API
├── translator/           # Utafsiri wa umbizo (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Zana zinazoshirikiwa (uwekaji kumbukumbu, hitilafu, mtiririko, n.k.)
```

### Idadi ya Moduli

| Saraka | Faili | Kusudi |
| `executors/` | 167 | Vitekelezaji vya HTTP kwa kila mtoa huduma (vilivyounganishwa kupitia kiwanda cha DefaultExecutor) |
| `handlers/` | 157 | Sehemu za kuingilia za maombi (chatCore, responses, embeddings) |
| `services/` | ~536 | Uelekezaji, uhifadhi wa muda, udhibiti wa kiwango, uonyeshaji upya, n.k. |
| `translator/` | 56 | Ugeuzaji wa umbizo (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | Zana na usafirishaji wa MCP |
| `utils/` | ~108 | Zana zinazohusika na maeneo mbalimbali (uwekaji kumbukumbu, hitilafu, mtiririko) |
| `config/` | ~339 | Mipangilio ya watoa huduma, thamani zisizobadilika, sajili |

---

## Mchakato wa Ombi

Kila ombi la LLM hupitia **mchakato wenye hatua 5**:

```
                              ┌──────────────┐
   Ombi la HTTP                │  1. ELEKEZA  │   utatuzi wa combo, uteuzi wa modeli
   (Njia ya Next.js)           └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. TAFSIRI  │   ugeuzaji wa umbizo (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. TEKELEZA │   kitekelezaji cha mtoa huduma, HTTP, jaribio upya, kikatiza
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. TIRIRISHA│   ugeuzaji wa SSE, shinikizo la nyuma
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. REKODI   │   ufuatiliaji wa matumizi, kumbukumbu ya mwito, uainishaji wa hitilafu
                              └──────┬───────┘
                                     │
                                     ▼
                              Jibu la HTTP (SSE au JSON)
```

### Hatua ya 1: Elekeza (services/combo.ts)

**Sehemu ya kuingilia**: `handleComboChat()` katika `services/combo.ts`

Hutatua ombi kuwa jozi halisi ya `(provider, model, account, credentials)`:

- Tafuta combo kwa ID (au unda combo pepe kwa modeli za `auto/*`)
- Tumia mkakati wa uelekezaji (kipaumbele, uzani, round-robin, n.k.)
- Ondoa watoa huduma wenye matatizo (kikatiza saketi)
- Chagua lengo linalofuata linalofaa

Kwa modeli za `auto/*`, hatua hii pia:

- Huendesha algoriti ya **upimaji wa vipengele 16** (`services/autoCombo/`)
- Huchagua jozi ya `provider+model` kulingana na hali, gharama, ukawiaji, n.k.

### Hatua ya 2: Tafsiri (translator/)

Ikiwa umbizo la chanzo (k.m., OpenAI) linatofautiana na umbizo lengwa (k.m., Claude), ombi **hutafsiriwa**:

- Kidokezo cha mfumo → ujumbe wa mfumo
- Ufafanuzi wa zana → umbizo la zana linalomhusu mtoa huduma
- Vigezo vya reasoning/thinking → vilingani vinavyomhusu mtoa huduma
- Usawazishaji wa jukumu la ujumbe (`developer` → `system` kwa wasio OpenAI)

`translator/index.ts` hufichua:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Hatua ya 3: Tekeleza (executors/)

**Sehemu ya kuingilia**: `getExecutor(providerId).execute(request, options)`

Watoa huduma wote hutumia `DefaultExecutor` (`executors/default.ts`) kupitia mbadala wa kiwanda cha `getExecutor()`. Kitekelezaji:

- Huunda URL ya upande wa juu (`buildUrl()`)
- Huongeza vichwa vinavyomhusu mtoa huduma (`buildHeaders()`)
- Hubadilisha mwili wa ombi (`transformRequest()`)
- Hutuma ombi la HTTP likiwa na jaribio upya + ongezeko la muda wa kusubiri
- Hushughulikia uonyeshaji upya wa uthibitishaji ikihitajika (watoa huduma wa OAuth)

Vitekelezaji vyote hurithi `BaseExecutor` (`executors/base.ts`, mistari 1170 ya msimbo) ambayo hutoa:

- Mantiki ya pamoja ya kujaribu upya
- Muunganisho wa proksi
- Muunganisho wa kikatiza saketi
- Vihusishi vya kurekodi matumizi

### Hatua ya 4: Tiririsha (utils/stream.ts)

Kwa majibu yanayotiririshwa, kitekelezaji hurejesha **ReadableStream**. Kishughulikiaji:

- Hupitisha kupitia kigeuzi cha SSE (`createSSETransformStreamWithLogger`)
- Hutumia mipigo ya heartbeat ili kugundua miunganisho iliyokufa
- Hushughulikia kukatika kwa muunganisho wa kiteja kwa ustadi (`pipeWithDisconnect`)
- Hubadilisha SSE → JSON kwa viteja visivyotumia utiririshaji

Kwa majibu yasiyotiririshwa, kitekelezaji hurejesha kipengee cha JSON kilichochanganuliwa ambacho hupitishwa bila kubadilishwa.

### Hatua ya 5: Rekodi (services/usage.ts)

Baada ya jibu (iwe limefanikiwa au limeshindwa), matumizi yanarekodiwa:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` kutoka kwenye jibu
- `cost_usd` inayokokotolewa kutoka kwenye data ya bei
- `latency_ms`, `status`, `error_class` ikiwa imeshindwa
- Yanahifadhiwa kwenye jedwali la `usage_history`

Vielelezo vya kumbukumbu za miito (ikiwa vimewezeshwa) huandikwa kwenye `${DATA_DIR}/call_logs/`.

---

## Uchambuzi wa Kina wa Faili Muhimu

### chatCore.ts (mistari 5977)

**Kishughulikiaji kikuu cha maombi**. Licha ya ukubwa wake, kina muundo ulio wazi:

```ts
// Muundo wa mfano wa chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Uthibitishaji + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Uthibitishaji wa body
  const body = await parseRequestBody(request);

  // 3. Utambuzi wa umbizo + utafsiri
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Uelekezaji wa combo
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Endelea hadi lengo linalofuata
    }
  }

  // 5. Njia mbadala ya dharura
  return await emergencyFallback(body);
}
```

Licha ya kuwa kitendakazi kimoja kikubwa, kimepangwa katika **sehemu zenye maoni** zinazolingana na mtiririko wa hatua 5.

### combo.ts (mistari 4456 ya msimbo)

**Injini ya uelekezaji** inayotatua combo kuwa malengo yaliyopangwa kwa mpangilio.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("lengo limeshindwa, inajaribu linalofuata", { target, err });
    }
  }
  throw new ComboExhaustedError("Malengo yote yameshindwa");
}
```

Inatumia **mikakati 19 ya uelekezaji** (tazama `src/shared/constants/routingStrategies.ts`):

| Mkakati             | Tabia                                                                 |
| ------------------- | --------------------------------------------------------------------- |
| `priority`          | Orodha iliyopangwa ikitanguliza lengo la kwanza                       |
| `weighted`          | Uwezekano kulingana na uzito wa kila lengo                            |
| `round-robin`       | Pitia malengo kwa mzunguko kwa mpangilio                              |
| `context-relay`     | Hamisha muktadha kati ya malengo                                      |
| `fill-first`        | Jaza mgao kabla ya kuhamia lengo linalofuata                          |
| `p2c`               | Nguvu ya chaguo mbili                                                 |
| `random`            | Uteuzi nasibu wenye uwezekano sawa                                    |
| `least-used`        | Chagua lenye matumizi machache zaidi ya hivi karibuni                 |
| `cost-optimized`    | Lengo lenye gharama ya chini zaidi na lililo imara kwanza             |
| `reset-aware`       | Inafahamu vipindi vya uwekaji upya vya mtoa huduma                    |
| `reset-window`      | Uelekezaji unaotegemea kipindi cha uwekaji upya                       |
| `headroom`          | Nafasi kubwa zaidi ya mgao uliosalia kwanza                           |
| `strict-random`     | Usawa halisi wa uwezekano (bila uzani wa ubora)                       |
| `auto`              | Tumia ukadiriaji wa vipengele 16 (`autoCombo/`)                       |
| `lkgp`              | Mtoa huduma wa mwisho anayejulikana kuwa mzuri kwanza                 |
| `context-optimized` | Bora zaidi kwa maombi yenye muktadha mrefu                            |
| `fusion`            | Tuma kwa jopo sambamba, kisha unganisha kupitia mwamuzi (`fusion.ts`) |

### base.ts (mistari 1170 ya msimbo)

**Kitekelezaji dhahania** ambacho vitekelezaji vyote 101 hurithi. Kina:

- `buildUrl()` — uundaji chaguomsingi wa URL (madaraja madogo hubatilisha kwa usanidi maalum)
- `buildHeaders()` — vichwa chaguomsingi (uthibitishaji, aina ya maudhui)
- `transformRequest()` — hupitisha bila mabadiliko kwa chaguomsingi
- `execute()` — kitanzi kikuu cha HTTP chenye kujaribu tena/kuchelewesha hatua kwa hatua/kikatiza mzunguko

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Hushughulikia watoa huduma wote wanaooana na OpenAI/Anthropic
  // Watoa huduma husajili usanidi (URL, uthibitishaji, vichwa) lakini hushiriki mantiki ya kitekelezaji
}
```

Tabia mahususi kwa kila mtoa huduma (vichwa vya uthibitishaji, URL msingi, vichwa vya toleo) husanidiwa kupitia sajili ya watoa huduma, si kupitia madaraja tofauti ya vitekelezaji.

````

---

## Huduma (moduli 117)

Huduma ni **moduli mahususi, zenye kusudi moja** ambazo vishughulikiaji huunganisha. Kategoria kuu ni:

### Uelekezaji na Mchanganyiko

- `combo.ts` — sehemu ya kuingilia kwa maombi yanayoelekezwa kwa mchanganyiko
- `services/autoCombo/` — uwekaji alama kwa vipengele 16, mikakati 8 ya uelekezaji otomatiki
- `wildcardRouter.ts` — hulinganisha njia zenye vibambo badala (`gpt-*`)
- `modelFamilyFallback.ts` — chaguo mbadala la T5 ndani ya familia

### Ukomo wa Kiwango na Mgao

- `rateLimitManager.ts` — ndoo ya tokeni kwa kila ufunguo+mtoa huduma
- `usage.ts` — kurekodi matumizi
- `quotaCache.ts` — vijipicha vya mgao vilivyo kwenye kumbukumbu

### Akaunti na Tokeni

- `tokenRefresh.ts` — uonyeshaji upya wa OAuth unapopokea 401
- `accountFallback.ts` — kubadilisha hadi akaunti mbadala
- `sessionManager.ts` — hali ya kipindi chenye zamu nyingi

### Uakili

- `intentClassifier.ts` — huainisha dhamira ya ombi
- `taskAwareRouter.ts` — huelekeza kulingana na aina ya kazi
- `thinkingBudget.ts` — hutenga tokeni za kufikiri
- `contextManager.ts` — huingiza muktadha wa uelekezaji

### Ustahimilivu

- `resilience.ts` — uratibu wa kujaribu tena, kusubiri kwa muda unaoongezeka, na kikataji
- `emergencyFallback.ts` — chaguo mbadala la mwisho
- `modelDeprecation.ts` — huelekeza kiotomatiki kwa modeli mbadala zinazofuata

### Hali

- `signatureCache.ts` — huondoa nakala kulingana na sahihi ya ombi
- `volumeDetector.ts` — upunguzaji wa mzigo
- `contextHandoff.ts` — usawazishaji wa kipindi

### Mfinyazo

- `compression/` (saraka ndogo) — mtiririko kamili wa mfinyazo
- Faili 39 zinazojumuisha injini, vifurushi vya kanuni na viadaptishaji

### Ujuzi

- (umeelezwa katika [SKILLS.md](./SKILLS.md))

### Kumbukumbu

- (imeelezwa katika [MEMORY.md](./MEMORY.md))

---

## Vitekelezaji (faili 75+)

Faili moja kwa kila mtoa huduma. Zote zinapanua `BaseExecutor` na kubatilisha kile kinachotofautiana.

### Miundo ya Kawaida

Watoa huduma hutatuliwa kupitia `getExecutor(providerId)`, ambayo hurejesha kitekelezaji kilichosanidiwa. Watoa huduma wanaooana na OpenAI/Anthropic hutumia `DefaultExecutor` (`executors/default.ts`). Tabia mahususi kwa mtoa huduma (URL ya msingi, vichwa vya uthibitishaji, toleo la API) husanidiwa katika `open-sse/config/providers/`, huku mabadiliko ya mwili wa ombi yakishughulikiwa katika `open-sse/translator/`.

**URL maalum** huwekwa kupitia usanidi wa mtoa huduma:

```ts
// Usanidi wa mtoa huduma katika open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Uthibitishaji maalum** hushughulikiwa kupitia usanidi wa uthibitishaji wa sajili ya watoa huduma (ufunguo wa API, OAuth, wasifu wa vichwa).

Mabadiliko ya **mwili maalum wa ombi** (k.m., Anthropic kutenganisha `system` na `messages`) husajiliwa kwa kila mtoa huduma katika `open-sse/translator/`.

````

### Kiwanda cha Vitekelezaji

`executors/index.ts` husafirisha `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Utatuzi hupitia `ExecutorRegistry` (`executors/registry.ts`): kila kitekelezaji maalum hutangazwa katika jedwali lililojengewa ndani la `executors/index.ts` na kusajiliwa kupitia `registerExecutor(alias, instance)` wakati wa kupakia moduli; `getExecutor()` hukagua sajili na kutumia `DefaultExecutor` iliyohifadhiwa kwa mtoa huduma yeyote asiye na ingizo maalum. Ulinganishaji kamili wa lakabu → kitekelezaji unafafanuliwa na jaribio la msingi `tests/unit/executor-map-golden.test.ts`.

---

## Watafsiri

Tafsiri kati ya **miundo 3**: OpenAI, Anthropic, Gemini, pamoja na Responses API mpya.

### Wakati Tafsiri Inapofanyika

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Tafsiri za kawaida:

- `OpenAI → Anthropic`: sehemu tofauti ya `system`, kichwa cha `x-api-key`
- `OpenAI → Gemini`: `contents` badala ya `messages`, `systemInstruction`
- `OpenAI → Responses API`: safu ya `input`, hali ya `previous_response_id`

### Hali Maalum Zinazoshughulikiwa

- Jukumu la `developer` → `system` kwa mifumo isiyo ya OpenAI
- Jukumu la `system` → huunganishwa katika ujumbe wa kwanza wa mtumiaji kwa GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` za Gemini
- `tools` → muundo wa zana unaotegemea mtoa huduma
- Vigezo vya kufikiri (o1, Claude) → thamani zinazolingana kulingana na mtoa huduma

---

## Seva ya MCP

`open-sse/mcp-server/` hutekeleza seva ya **Model Context Protocol**:

- **Zana 110** (usimamizi wa watoa huduma, michanganyiko, kumbukumbu, akiba, mgandamizo, proksi, ujuzi, uundaji wa uzoefu kama mchezo, programu-jalizi, Notion, Obsidian, hazina ya ndani)
- **Njia 3 za usafirishaji**: stdio, SSE, Streamable HTTP
- **Mawanda 33** kwa ajili ya uidhinishaji wa kina

### Usajili wa Zana

Zana husajiliwa kama faili zinazojitegemea katika `open-sse/mcp-server/tools/`, ambapo kila moja husafirisha jina, schema, kidhibiti na wigo:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Njia za Usafirishaji

```ts
// stdio (matumizi ya CLI)
startMcpStdio(server);

// SSE (utiririshaji unaotegemea HTTP)
startMcpSse(server, port);

// Streamable HTTP (MCP ya kisasa)
startMcpStreamable(server, port);
```

### Uidhinishaji

Kila mwito wa zana hupitia ukaguzi wa wigo (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Vigeuzi

`open-sse/transformer/` hubadilisha kati ya miundo ya **Chat Completions** na **Responses API**.

### Kwa Nini Kigeuzi Tofauti?

Responses API ni muundo mpya wa OpenAI wenye **mazungumzo yanayohifadhi hali** (`previous_response_id`). Mteja anapotuma ombi la Responses, OmniRoute:

1. Hubadilisha Responses → Chat Completions kwa matumizi ya ndani
2. Hutuma kwa mtoa huduma (mtoa huduma yeyote anayetumia Chat Completions)
3. Hubadilisha jibu kurudi katika muundo wa Responses
4. Hutiririsha jibu lililobadilishwa kwa mteja

Kigeuzi (`transformer/responsesTransformer.ts`) hutoa:

```ts
createResponsesApiTransformStream(): TransformStream
```

Hiki hushughulikia:

- Matukio ya `response.output_item.added`
- Matukio ya `response.output_text.delta`
- Tukio la `response.completed`
- Ulinganishaji wa miito ya zana (`function_call` ↔ `tool_calls`)

---

## Usanidi

`open-sse/config/` huhifadhi safu ya usanidi:

| Faili                             | Madhumuni                                                     |
| --------------------------------- | ------------------------------------------------------------- |
| `providerRegistry.ts`             | Sajili ya modeli za gumzo juu ya katalogi ya watoa huduma 352 |
| `providerModels.ts`               | Majina mbadala ya modeli, ulinganishaji wa miundo             |
| `constants.ts`                    | Vikomo vya muda, vikomo, misimbo ya hali                      |
| `defaultThinkingSignature.ts`     | Sahihi chaguomsingi ya kufikiri ya Claude                     |
| `modelStrip.ts` (katika services) | Uondoaji wa sehemu kulingana na mtoa huduma                   |

### Schema ya Sajili ya Watoa Huduma

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Uthibitishaji wa Zod wakati wa kupakia moduli huhakikisha usanidi wote wa watoa huduma ni halali.

---

## Vikwazo vya Utendaji

Injini ya uelekezaji ina viwango vikali vya utendaji:

| Operesheni                                                          | Lengo | Kipimo                                          |
| ------------------------------------------------------------------- | ----- | ----------------------------------------------- |
| Utatuzi wa combo                                                    | <10ms | Kwa malengo 50                                  |
| Ukaguzi wa kikomo cha kiwango                                       | <1ms  | Token bucket ya ndani ya kumbukumbu             |
| Fallback ya familia ya modeli                                       | <5ms  | Ufafanuzi wa familia uliohifadhiwa kwenye cache |
| Usambazaji wa ombi kwa njia husika                                  | <2ms  | Njia inayotumika sana                           |
| **Hakuna I/O inayozuia katika njia inayotumika sana ya uelekezaji** | —     | Yote ni asinkronasi                             |

---

## Mifumo Isiyofaa

❌ **Miito ya DB ya kisinkronasi katika `combo.ts`** — kokotoa mapema na uhifadhi kwenye cache
❌ **Mantiki ya kujaribu tena katika handlers** — tumia `retry()` kutoka kwenye huduma ya ustahimilivu
❌ **Ufikiaji wa moja kwa moja wa usanidi wa provider** — tumia getters za `providerRegistry`
❌ **Minyororo ya fallback iliyoandikwa moja kwa moja** — ifafanue katika `modelFamilyFallback.ts`
❌ **Mabadiliko ya hali kati ya maombi yanayotekelezwa kwa wakati mmoja** — tumia tu muktadha maalumu wa ombi

---

## Kuongeza Kipengele Kipya

### Kuongeza Huduma Mpya

1. Unda `open-sse/services/[serviceName].ts` yenye jukumu mahususi
2. Hamisha function kuu ya handler na constants zozote
3. Ongeza majaribio ya unit katika `tests/unit/services/[serviceName].test.mjs`
4. Ijumuishe kwenye mtiririko wa ombi katika `handlers/chatCore.ts` (ikiwa inahusiana na uelekezaji)
5. Sasisha mantiki ya uelekezaji katika `combo.ts` ikiwa huduma inaathiri uteuzi wa lengo
6. Iandike katika faili hii

### Kuongeza Executor Mpya

1. Unda `open-sse/executors/[provider].ts` inayopanua `BaseExecutor`
2. Isajili katika `config/providerRegistry.ts`
3. Iongeze kwenye factory ya `executors/index.ts`
4. Ongeza majaribio ya unit kwa executor
5. Iandike katika `docs/architecture/ARCHITECTURE.md`

### Kuongeza Zana Mpya ya MCP

1. Unda au sasisha `open-sse/mcp-server/tools/[category]Tools.ts`
2. Fafanua schema ya Zod kwa inputs
3. Sajili zana katika `mcp-server/index.ts`
4. Iongeze kwenye matrix ya scope katika `mcp-server/auth/`
5. Ongeza majaribio ya unit

---

## Tazama Pia

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — usanifu wa kiwango cha juu
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — rejea ya uhandisi
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — maelezo ya kila directory
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — ukadiriaji wa vipengele 16
- [MCP-SERVER.md](./MCP-SERVER.md) — seva ya MCP
- [A2A-SERVER.md](./A2A-SERVER.md) — seva ya A2A
- Chanzo: `open-sse/` (faili 400+, ~143K LOC)
