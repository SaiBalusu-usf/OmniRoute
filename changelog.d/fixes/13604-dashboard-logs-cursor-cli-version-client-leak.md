- **fix(build):** keep `node:fs`/`node:os`/`node:path` out of the client chunk graph for `/dashboard/logs` — a further, previously-undiscovered edge (`src/lib/oauth/constants/oauth.ts` importing `cursorAgentCliVersion.ts`, reachable from 250+ dashboard pages) of the same root cause already fixed in #13436/#13509/#13568. Moves the pinned CLI version string into a new import-free leaf module and extends the `client-bundle-no-server-only-10692` guard test to cover it ([#13604](https://github.com/diegosouzapw/OmniRoute/pull/13604)).
