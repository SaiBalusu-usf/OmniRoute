- **fix(usage):** Claude OAuth usage reads the per-model weekly caps Anthropic now reports in `limits[]` (`weekly_scoped`, e.g. Fable) and maps them into `weekly <model> (7d)` quotas, so a scoped cap that is the active limit on the seat is no longer invisible to the dashboard, the Provider Limits cache and fair-share saturation ([#13962](https://github.com/diegosouzapw/OmniRoute/pull/13962)) — thanks @dpozimski
