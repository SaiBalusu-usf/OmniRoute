- **test(batches):** the two seeded-batch labels of the delete-completed route-scope suite are renamed to short literals (`route401`/`route500`) so a git-mode gitleaks scan of a branch touching that file no longer reports them as `generic-api-key` hits; the CI secret ratchet never scanned `tests/`, so nothing changes there ([#13729](https://github.com/diegosouzapw/OmniRoute/pull/13729))
