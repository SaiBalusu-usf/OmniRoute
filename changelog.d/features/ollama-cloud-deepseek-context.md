- **feat(providers):** register `deepseek-v4.1-flash` and declare the operator's 262144 working cap as `contextLength` on all three ollama-cloud DeepSeek v4 models (`deepseek-v4-pro`, `deepseek-v4-flash`, `deepseek-v4.1-flash`). These models natively serve 1M, but the deliberate working cap for agentic use is 262144 (`config/routers/omniroute.yml`: "262144 is the deliberate working cap for agentic use — sufficient with pruning, bounds per-turn input cost ... a CHOICE, not a provider limit"). Previously ollama-cloud shipped **no** `defaultContextLength` and **no** per-model `contextLength`, so `resolveTokenLimit` (contextManager.ts) fell through to `DEFAULT_LIMITS.default = 128000` — TOO LOW, not the operator's 262144 — and OmniRoute 400'd `"Input exceeds context window ... limit 128000"` on >128k prompts that ollama-cloud would have served. **Convention for future model additions:** any model on a `passthroughModels: true` / registry provider MUST declare its real (operator-capped) `contextLength` (and the provider a `defaultContextLength`) — never rely on the 128k `DEFAULT_LIMITS` catch-all, which false-limits every large-window model not explicitly annotated. Declare the 262k working cap, not the 1M native window. Verify with a direct >128k probe after deploy (`bun` + `OMNIROUTER_API_KEY`, `User-Agent: curl/8.7.1`) — it must now pass to the 262k cap.
